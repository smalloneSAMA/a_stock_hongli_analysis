# 2026-09-08 需求设计方案

> **状态**：决策已确认（本文档只做设计，**不改任何代码**）
> **依据**：`docs/20260908优化方向分析.md`（第 2 版，已含北交所剔除决策）
> **数据基线**：`manifest.data_date = 2026-09-08`
> **修订记录**：
> - 第 1 版：初稿，R1–R9 + 6 项待确认决策
> - **第 2 版（本次）**：D1–D5 与"推荐 20 补位"已拍板 → 更新 §2 范围、§3 R5/R9、§4 决策记录、§5 契约、§6 影响面、§8 批次、§9 风险、§10 待澄清
> **编制原则**：每条需求可验收；每个取舍显式列出；不引入未被要求的能力

---

## 1. 背景与目标

现状（实测）：工作树 398.7 MB、`.git` 584.55 MiB（210 commits / 5 周）、三个主视图首屏加载 31–32 MB 未压缩 JSON、`analysis_dy.json` 30.7 MB 中 99.7% 前端不读、日常更新 5–8 分钟（其中 248 s 是纯 sleep）。

| 目标 | 量化指标 |
| :--- | :--- |
| G1 数据陈旧可见 | 任一标的滞后 >2 交易日必须出现在 `status` 与失败清单 |
| G2 主视图首屏瘦身 | 31.4 / 32.3 MB → **< 1 MB** |
| G3 交付层压缩 | JSON 传输体积降至 **10%–20%** |
| G4 落盘体积 | 单只股票传输 ~900 KB → **~100 KB**；`analysis_dy.json` 30.7 MB → **~1.8 MB** |
| G5 仓库增长 | 84 MB/月 → **< 20 MB/月**；噪声 commit 归零 |
| G6 单一来源 | 前缀 / `pct_rank` / `IDX_DIV` / `UA` / ETF 元数据各 **1 份** |
| G7 CI 护栏 | 新增回归能在 CI 变红 |

---

## 2. 范围

**本期做**：R1–R9（**R9 已纳入本期**；**R5 全部步骤 5a–5d**）。

**本期不做**：引入 bundler；虚拟滚动；`web/data` 整目录事务化；前端大规模重构；数据迁 orphan 分支（R6 仅做 ①②）。

**前提决策（已定）**：北交所标的**个股池剔除、指数成分记录保留**。

---

## 3. 需求条目

### R1 数据新鲜度可观测（P0）

| 项 | 内容 |
| :--- | :--- |
| 现状 | `_fetch_history.py:207-232` 收到 `[]` 仍写 `fetched_at=today`；`update.py:148-157` 每组只抽查 1 只；`_gen_web_data.py:179` 用 `max(dates)` 掩盖单只滞后 |
| 需求 | ① 抓取"空返回 + 落后基准 >3 交易日"判定为**失败**并入失败清单；② `status` 每组输出 min/中位/max 日期 + 滞后 >2 交易日的 code 清单；③ `manifest.json` 每标的增加 `ind_last`（指标末日期）与 `stale` 布尔 |
| 验收 | 临时截断某标的 `cache/股票_*.json` 的 `rows` → `python update.py status` 报出该 code；失败清单出现记录；前端显示"数据滞后"标记 |

### R2 北交所标的剔除（P0）

| 项 | 内容 |
| :--- | :--- |
| 现状 | 全项目仅 2 只（920599 同力股份、920509 同惠电子），来自中证红利价值 1.39% / 中证全指红利质量 0.04% |
| 需求 | ① 个股池（评分候选 / K线分红财报抓取 / 前端个股列表 / 回测分组 / 持仓可选项）不含北交所；② 不再对其发起任何抓取请求；③ 清理既有缓存与产物 |
| 设计 | `scripts/_common.py` 新增唯一判定 `is_bj(code)`；在**池构造点**统一调用（`_recommend_stocks.py` 为权威点，其余下游自然跟随） |
| **补位规则（已定）** | 剔除 920599 后，推荐 20 由**备选（backup 段）补位至 20 只**，不缩减名单 |
| 验收 | `python update.py daily` 日志无 920599/920509 请求；`manifest.stocks` 364→362；`_成分股汇总表.json` 329→327；`cache/股票_*.json` 376→374；`_推荐20.json` list 仍为 20 且不含北交所；`_test_analysis.py` 全绿 |
| 清理清单 | 见 `docs/20260908优化方向分析.md` 附录 C（8 个个股缓存 + 2 个前端指标 + 6 类产物 + 1 个 Excel sheet + md 6 行） |

### R3 三主视图数据加载瘦身（P0）

| 项 | 内容 |
| :--- | :--- |
| 现状 | `scanView.js:12,19`、`recommendView.js:13,20`、`holdingsView.js:14,24` 全量加载 `/cache/analysis_dy.json`（30.7 MB），实际只用 5 个标量字段 + 一个 maxDate |
| **设计要点** | **不新增请求**：把这 5 个字段并入三视图**本来就要加载**的 `web/data/analysis.json` |
| 数据契约 | `analysis.json.by_code[code]` 现有 `name / type / track / factors / anchors / dy_p10 / dy_p90 / win_start / n_days / dy_series`，**新增** `dy_now`、`dy_pct`、`dy_p50`、`close_now`（`_gen_analysis.py` 同一趟已算出） |
| 前端改动 | 三视图删除 `DY_URL` 常量与加载；`reco.js:38 buildRecoPool(dy, …)` 改为遍历 `an.by_code`；maxDate 改用 `an.date` / `m.data_date` |
| compareView | 唯一真正需要 series 的视图（仅指数/ETF）。`_gen_analysis.py` 另出 `web/data/dy_series.json`（**只含 11 指数 + 12 ETF**，约 1.7 MB → gzip 约 250 KB），`compareView.js:243,276-277` 改读它 |
| 验收 | 浏览器 Network 确认三视图**无** `analysis_dy.json` 请求；三视图每行数值与改前逐字段一致 |
| 收益 | 三视图首屏 31.4 / 32.3 MB → **0.66 MB**；compareView 33 MB → 2.3 MB |

### R4 交付层压缩与缓存（P0）

| 项 | 内容 |
| :--- | :--- |
| 现状 | `serve.py:52-56` 全量 `no-store`；无任何 gzip（实测 `Accept-Encoding: gzip` 仍返回原尺寸） |
| 需求 | ① `.json/.js/.css` 启用 gzip（>1 KB 才压，按 `Accept-Encoding` 协商）；② `/cache`、`/web/data` 改 `Cache-Control: no-cache` + `ETag`（内容哈希），命中则 304；③ `web/js`、`web/css` 保持 `no-store` |
| 验收 | `curl -sI -H "Accept-Encoding: gzip" /cache/analysis_dy.json` 出现 `Content-Encoding: gzip`；二次请求 304；数据重生成后 ETag 变化、刷新拿到新值 |
| 风险 | 缓存策略写错会读旧数据 → 必须用 ETag 强校验（非 `max-age`） |

### R5 落盘体积压缩（P1）—— **全部步骤本期执行**

| 步骤 | 内容 | 预期收益 | 依赖 |
| :--- | :--- | :--- | :--- |
| **5a** | 指标文件去掉重复日期列 `d`，按索引与 K 线对齐 | 省 13.9 MB 原始；每只前端文件 −26% | 已验证可行 |
| **5b** | 列式编码 `{cols:[…], rows:[[…]]}` | `cache/股票_600036.json` 820 KB → 412 KB | 5a |
| **5c** | `roe/roa` 改稀疏"变更点"表示（实测 98.5% / 98.4% 行与上一行相同） | 省约 11 MB | 5b |
| **5d** | 后端改读指标文件的 `dy` 列，`analysis_dy.json` 去掉股票 series | `analysis_dy.json` 30.7 MB → **约 1.8 MB** | 5b/5c（共用解码逻辑） |

**已验证前提（本次补充）**：376 个 `web/data/stocks/*.json` 与对应 `cache/股票_*.json` **长度与首末日期逐行一致（376/376 全部对齐）**，故 5a 的"按索引对齐"成立。

| 项 | 内容 |
| :--- | :--- |
| 影响面 | 前端读取路径需同步：`historyLayout.js`（K线+指标合并）、`compareView.js`（dy/分红序列）、`charts.js`（系列构建）；后端 `_backtest_analysis.merge_close`、`_backtest_portfolio`（读 `dy_ser`） |
| 验收 | 每一步跑 `_test_analysis.py` 全绿 + 逐字段数值对账（改前后 diff 为空）；5d 后确认 `update.py bt` 产物与改前一致 |
| 风险 | 格式变更遗漏读取点 → 先 grep 全部 `.rows` / `r.d` / `rows[` 读取点再动；每步独立 commit 便于回滚 |

### R6 仓库增长治理（P1）—— **本期只做 ①②**

| 项 | 内容 |
| :--- | :--- |
| 现状 | `.git` pack 584.55 MiB；跨天运行时 `fetched_at` 让全部 770 个数据文件都变（`a8d1bac6` 777 文件 vs 同日重跑 `666f6484` 34 文件），使 CI 的 `git diff --quiet` 无变化守卫**永不触发** |
| 需求 ① | `update_incremental` 仅在 `rows` 真变化时写盘，`fetched_at` 仅在写盘时更新 |
| 需求 ② | 清理非项目跟踪文件（`docs/_zhaomu.pdf` 1.27 MB、`_cons_tmp/` 18 个 xls）与孤儿缓存（12 只 → 10 只） |
| **不做** | 数据迁 orphan 分支（D4：本期不做） |
| 验收 | 非交易日跑完 `python update.py daily` 后 `git status` **干净**；一个月后 `git count-objects -vH` 增量 < 20 MB |

### R7 单一来源收敛（P1）

| 项 | 内容 |
| :--- | :--- |
| 现状 | 前缀推导 10 处、`pct_rank` 3 份、`IDX_DIV` 3 份、`UA` 7 份、ETF 元数据 5 份、`_FALLBACK_FINAL20` 3 份、`update.py` 重复实现 `_common` |
| 需求 | 各收进 `scripts/_common.py` 单一实现；`update.py` 改调公共函数；抽 `rebuild_web()` 消除 6 份复制；清 `FCF_ETFS` 的退役 159229 与 `_full_backfill.py` 的北交所硬编码 |
| **口径决策（已定）** | `pct_rank` 统一为 **`<`**（与 `_gen_analysis.py:51` 一致） |
| 验收 | `python scripts/_common.py` 自测通过；`_test_analysis.py` 全绿（统一后分位微调 → 快照按惯例刷新并注明漂移） |

### R8 工程化护栏（P1）

| 项 | 内容 |
| :--- | :--- |
| 现状 | 62 项断言集中在 Python 侧且靠 `--no-snapshot` 绕开脆弱快照；`_common.py` 自带 12 项离线自测但 CI 未跑；无 lint/type 配置；前端纯函数无回归网 |
| 需求 | ① CI 增加 `python scripts/_common.py`；② 新增 3 条关系断言：`len(推荐20 分组行数) == len(_推荐20.json.list)`、TOP20 因子覆盖率 ≥7/10、`manifest.stocks` 与 `cache/股票_*.json` 一一对应；③ 引入 `ruff --select F,E9,B`；④ `node --test` 覆盖 `analysis.js` / `reco.js` 纯函数 |
| 验收 | 人为制造 R1/R2 类回归 → CI 变红；`ruff` 首次运行能报出 `_fetch_stock_data.py:20` 未用的 `random`、`_backtest_analysis.py:13` 未用的 `io` |

### R9 回测执行日课题（P0 · **本期执行**）

| 项 | 内容 |
| :--- | :--- |
| 现状 | `_backtest_analysis.py:63-64` t 日确认信号，`:73` 用 `close[i]` 买入 → 与 `docs/回测报告.md:4` 宣称的"次一交易日"差 1 天 |
| 需求 ① | 新增开关（不改默认）以 **+2 口径**重跑 `update.py bt` + 组合回测，产出对比报告（指数 / ETF / 推荐20 的 1/3/6/12M 超额变化） |
| 需求 ② | **根据报告结果决定是否修正默认口径**——修正会改所有回测产物与"指数 12M 超额 +24%"等结论，需看到数据后单独确认 |
| 验收 | 报告给出"12M 超额 +24% → ?"的对比与是否反转的结论；②的决策单独记录 |
| 说明 | 需求 ② 是**报告产出后的决策门**，不在本批次直接改默认口径（口径变更须独立提交并注明） |

---

## 4. 决策记录（已确认）

| 编号 | 决策 | 确认结论 | 影响 |
| :--- | :--- | :--- | :--- |
| **D1** | 北交所在指数成分记录中是否保留 | **保留**，标注"无行情数据、不参与计算" | 指数权重和与官方样本一致；成分表存在点不开的行 |
| **D2** | R5 做到哪一步 | **全部做（5a+5b+5c+5d）** | 单只传输 ~900 KB → ~100 KB；`analysis_dy.json` → ~1.8 MB；需同步改前端 3 处读取路径与后端 2 处 |
| **D3** | `pct_rank` 统一口径 | **`<`** | 推荐评分与组合回测分位微调 → 快照刷新并注明 |
| **D4** | 数据是否迁 orphan 分支 | **本期只做 R6 ①②** | 保留"缓存随 git 提交、误删可恢复"现状 |
| **D5** | R9 是否本期执行 | **本期执行**（出对比报告 + 报告后决策门） | 默认口径暂不变，结论待人工判断 |
| **D6** | 剔除 920599 后推荐 20 规模 | **由备选补位至 20 只** | `_推荐20.json` list 保持 20，不含北交所 |

---

## 5. 数据契约变更汇总

| 产物 | 变更 | 兼容性 |
| :--- | :--- | :--- |
| `web/data/analysis.json` | `by_code[code]` 新增 `dy_now / dy_pct / dy_p50 / close_now` | 纯新增字段，旧前端不受影响 |
| `web/data/dy_series.json` | **新文件**：`{date, by_code: {code: [[date, dy], …]}}`，仅 11 指数 + 12 ETF | 新文件，仅 `compareView` 使用 |
| `web/data/manifest.json` | 每标的新增 `ind_last`、`stale`；移除北交所 2 条 | 旧字段不变 |
| `web/data/stocks/*.json` | **5a** 去 `d` 列（按索引对齐）；**5b** 列式编码；**5c** `roe/roa` 稀疏化 | **破坏性**，前端须同步 |
| `cache/股票_*.json` | **5b** 列式编码 | **破坏性**，前端与后端须同步 |
| `cache/analysis_dy.json` | **5d** 去掉股票 series（仅留标量 + 指数/ETF series） | 后端 `merge_close` / `dy_ser` 改读指标文件 |
| 所有产物 | 统一加 `schema_version` | 新增字段 |

---

## 6. 影响面矩阵

| 需求 | 主要改动文件 | 产物变化 | 口径是否变化 |
| :--- | :--- | :--- | :--- |
| R1 | `_fetch_history.py`、`update.py`、`_gen_web_data.py` | manifest 加字段 | 否 |
| R2 | `_common.py`、`_recommend_stocks.py`、`_fetch_*.py`、`_gen_*.py`、`_update_summary.py` | 池 −2；推荐20 补位保持 20 | **是**（名单换血、指数加权股息率微变） |
| R3 | `_gen_analysis.py`、`scanView.js`、`recommendView.js`、`holdingsView.js`、`reco.js`、`compareView.js` | analysis.json +约 20 KB；新增 dy_series.json | 否 |
| R4 | `serve.py` | — | 否 |
| R5 | `_gen_web_data.py`、`_gen_analysis.py`、`_backtest_analysis.py`、`_backtest_portfolio.py`、`historyLayout.js`、`charts.js`、`compareView.js` | 格式与体积大幅变化 | 否（需逐字段对账） |
| R6 | `_fetch_history.py`、`.gitignore` | 写盘时机 | 否 |
| R7 | `_common.py` 及全部调用方 | — | **`pct_rank` 统一为 `<` 会微调分位** |
| R8 | `.github/workflows/daily-update.yml`、`_test_analysis.py`、新增 `pyproject.toml` / `tests/` | — | 否 |
| R9 | `_backtest_analysis.py`（新增开关） | 新增对比报告 | 默认不变；若②决策通过则**全量口径变更** |

---

## 7. 验证方案（分层）

| 层 | 手段 | 适用 |
| :--- | :--- | :--- |
| L1 语法 | Python 直接运行；JS 转 `.mjs` 后 `node --check` | 全部 |
| L2 单测 | `python scripts/_common.py`（12 项）、`python scripts/_test_analysis.py`（60 项，CI 用 `--no-snapshot`）、新增 `node --test` | R7/R8 |
| L3 数值对账 | 改前后逐字段 diff（产物 vs 基线快照） | R3/R5/R7/R9 |
| L4 端到端 | `python update.py daily --yes` + `python update.py status` | R1/R2/R6 |
| L5 浏览器 | playwright-core + 系统 Edge headless：`getOption()` 探测 legend/series；Network 断言无 `analysis_dy.json` 请求 | R3/R4/R5 |
| L6 演练 | 人为截断某标的数据 → 验证 R1；人为造 BJ 标的 → 验证 R2 | R1/R2 |

---

## 8. 实施批次与提交计划

| 批次 | 需求 | 依赖 | 风险 |
| :--- | :--- | :--- | :--- |
| **B1** | R1 数据新鲜度 | — | 低 |
| **B2** | R2 北交所剔除（含补位至 20） | B1（同批改 `_fetch_*`） | 低-中（名单换血，按口径变更提交并注明漂移） |
| **B3** | R3 瘦身 + R4 压缩 | — | 低（可并行） |
| **B4** | R5 全量（5a→5b→5c→5d，逐步提交） | B3 | **中-高**（格式破坏性变更，前端 3 处 + 后端 2 处同步） |
| **B5** | R6 ①② | — | 低 |
| **B6** | R7 单一来源收敛（`pct_rank` 统一为 `<`） | B2 | 低-中（快照刷新） |
| **B7** | R8 工程化护栏 | B1–B6 | 低 |
| **B8** | R9 对比报告（+ 报告后决策门） | B4（若 5d 改了产物格式，先跑通再对比） | 中（可能改结论） |

每个 commit：中文主题 + 要点分列（根因 / 修复 / 验证）；涉及数据漂移或格式变更的提交需注明。

---

## 9. 风险与回滚

| 风险 | 缓解 |
| :--- | :--- |
| R3 改读来源后三视图数值不一致 | 上线前逐字段对账；浏览器 Network 断言 |
| R2 剔除导致推荐名单换血 | 按"口径变更"单独提交，人工确认补位后的新 TOP20；回测产物同步重跑 |
| R4 缓存策略误配读到旧数据 | 只用 ETag 强校验，不用 `max-age`；`?v=data_date` 兜底 |
| **R5 格式破坏性变更遗漏读取点** | 先 grep 全部 `.rows` / `r.d` / `rows[` 读取点；5a→5d 逐步提交，每步对账 + 可独立回滚 |
| R7 `pct_rank` 统一引起分数微调 | 快照刷新并注释；`docs/回测报告.md` 重跑 |
| R9 结论反转 | 先出报告再决策；口径变更独立提交 |
| 数据回滚 | `cache/` 与 `web/data/` 均随 git 提交，`git checkout -- cache/ web/data/` 即可恢复 |

---

## 10. 待澄清（2 项）

1. **R5d 是否包含在"R5 全部做"内**：R5d（`analysis_dy.json` 去掉股票 series，30.7 MB → ~1.8 MB）在第 1 版中标注为"可选"，本次按"全部做"**已纳入 B4**；若你的意思是只做 5a–5c，请指出，B4 可缩为三步。
2. **R9 需求 ② 的时点**：本期先出对比报告，**是否修正默认口径**留到报告出来后决策（因为会改所有回测产物与结论）；若你希望本期直接以新口径为准，请指出，B8 会增加"默认口径切换 + 全量产物重跑"。

---

*本文档为设计方案，未修改任何代码或数据。*
