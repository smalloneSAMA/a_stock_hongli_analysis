# 2026-09-08 执行方案

> **依据**：`docs/20260908需求设计方案.md`（第 2 版，D1–D6 已确认）
> **状态**：待执行（**本文档不改任何代码**）
> **用法**：按 B1 → B8 顺序推进；每步含 **改动点 / 做法 / 命令 / 验证 / 数据漂移 / 回滚**，一步一 commit
> **数据基线**：`manifest.data_date = 2026-09-08`

---

## 0. 执行前准备（P0 基线）

**目的**：建立可回滚、可对账的起点。

| 步骤 | 命令 | 预期 |
| :--- | :--- | :--- |
| 0-1 记录起点 | `git rev-parse HEAD` → 记入本方案附录 | 得到基线 commit |
| 0-2 确认工作区干净 | `git status --short` | 仅未跟踪的 2 个 docs 文件 |
| 0-3 测试基线 | `python scripts/_test_analysis.py` | 60 项全绿（T4.5/T7 快照按当日行情） |
| 0-4 状态基线 | `python update.py status` | 各组日期一致、无失败清单 |
| 0-5 产物快照 | 把 `web/data/{manifest,analysis,backtest,summary}.json` + `cache/analysis_dy.json` 复制到 `.baseline/`（git 忽略） | 供 L3 对账 |
| 0-6 池规模基线 | 记录 `manifest.stocks`=364、`analysis.by_code`=385、`_成分股汇总表`=329、`cache/股票_*`=376 | 供 B2 验收 |

**约定**：

- 每步独立 commit，中文主题 + 要点分列（根因 / 修复 / 验证）；涉及数据漂移的注明。
- JS 改动一律转 `.mjs` 后 `node --check`（AGENTS.md §7）。
- 自动化验证在受限沙箱内执行 python 需 `cmd /c "python …"` 包装（`困难总结 #149`）；人工执行用普通命令即可。
- **每批次结束**跑一次 `python scripts/_test_analysis.py`。

---

## B1 · 数据新鲜度可观测（对应 R1）

### B1-1 抓取"空返回"判失败

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_fetch_history.py:198-232`（`update_incremental`） |
| 做法 | ① 函数返回值保持 `(n_new, n_total, last_date)`；② 新增入参 `baseline=None`：当 `n_new == 0` 且 `baseline` 存在 且 `last_date < baseline - 5 自然日`（≈3 交易日，注释说明近似）时，抛 `StaleDataError`（新增异常类，定义在 `_common.py`） |
| 调用方 | `_fetch_pool_data.py:101-120`、`_fetch_stock_data.py:562-573`、`_fetch_watchlist.py:213-264`：捕获 `StaleDataError` → 记入各自失败清单（现有 `_pool_failed.json` / `_watchlist_failed.json` 机制），**不中断整轮** |
| 基准来源 | 该轮运行内所有标的 `last_date` 的**最大值**（自包含，不引入交易日历依赖）；实现方式：先在内存收集，再对落后项二次判定 |
| 命令 | `python scripts/_fetch_pool_data.py` |
| 验证 | ① 正常日：日志无新增失败项；② 演练：把某标的 `cache/股票_600036.json` 的 `rows` 截断到 30 天前 → 重跑 → 该 code 出现在 `cache/_pool_failed.json`；③ `python update.py status` 的失败清单计数 +1 |
| 漂移 | 无 |
| 回滚 | `git revert` 本 commit |

### B1-2 `status` 每组 min/中位/max + 滞后清单

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `update.py:144-210`（`scan_status`） |
| 做法 | 把 `:148-157` 的 `pick()`（每组取第一只）改为收集**该组全部标的**的 `last`：输出 `min / 中位 / max` 三个日期；追加一行列出"滞后 >2 交易日的 code"（沿用 B1-1 的自然日近似阈值） |
| 命令 | `python update.py status` |
| 验证 | 演练截断某标的后，输出出现 `推荐20: 2026-08-24 ~ 2026-09-08 ⚠️ 滞后 1 只（600036）` 之类 |
| 漂移 | 无 |

### B1-3 manifest 增加 `ind_last` / `stale`

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_gen_web_data.py:50-186`（`build_manifest`），配合 `:240-278`（`build_stock_indicators`） |
| 做法 | 每标的增加 `ind_last`（该标的指标文件末行日期，落盘时顺带返回，避免再读一遍 155 MB）与 `stale`（`ind_last` 落后全局 `data_date` >2 交易日）。同时把 `:179` 的 `data_date = max(dates)` 改为**指数/ETF 优先**的基准（避免单只滞后被 max 掩盖） |
| 命令 | `python update.py web` |
| 验证 | `python -c` 抽查 manifest：`stale` 字段存在且 920599 类滞后项为 true；`data_date` 与 `analysis.date` 一致 |
| 漂移 | manifest 体积 +约 20 KB |

### B1-4 前端显示"数据滞后"标记

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `web/js/views/historyLayout.js:396`（`renderMain` 头部）、`web/js/views/common.js`（可复用角标组件） |
| 做法 | 读 `manifest` 中该 code 的 `stale`，在标的标题旁渲染灰色角标"数据滞后（截至 YYYY-MM-DD）"；无字段时不渲染（向后兼容） |
| 验证 | `node --check`（转 `.mjs`）+ Edge headless：截断某标的后打开其 K 线页，角标出现 |
| 漂移 | 无 |

**B1 验收**：G1 达成——任一滞后标的在 `status`、失败清单、manifest、前端四处可见。

---

## B2 · 北交所标的剔除（对应 R2）

### B2-1 新增唯一判定

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_common.py`（在 `market_prefix` 附近） |
| 做法 | `def is_bj(code): return code.startswith(("43","83","87","88","92"))`；在 `__main__` 冒烟自测加 4 条用例（`920599/430047/830799` → True，`600036/000858/510880` → False） |
| 命令 | `python scripts/_common.py` |
| 验证 | 自测由 12 项 → 16 项，全绿 |

### B2-2 池构造点统一过滤（6 处）

| 文件:行号 | 改动 |
| :--- | :--- |
| `scripts/_recommend_stocks.py:297-309` | 硬过滤循环内加分支：`elif is_bj(c): why = "北交所(不纳入)"` —— **权威点** |
| `scripts/_fetch_stock_data.py:57-75` | `_rec_stocks()` 过滤；并清掉 `_FALLBACK_STOCKS:33` 中的北交所项 |
| `scripts/_fetch_pool_data.py:33-43` | `pool_codes()` 过滤 |
| `scripts/_gen_web_data.py:223`（`stock_pool`）、`:240`（`build_stock_indicators`） | 过滤 |
| `scripts/_gen_analysis.py:369-377`（`enough_history`） | 前置过滤 |
| `scripts/_update_summary.py:157-178`（`build_table`） | 过滤 |
| `scripts/_fetch_watchlist.py:214-217` | 加护栏（当前 51 只自选股无北交所，防未来误加） |

**说明（D1）**：**指数成分记录保留**——`_gen_components.py` 与 `components.json` **不改**，920599/920509 仍作为成分出现，但标注"无行情数据、不参与计算"；前端成分表中这两行不可点开。

### B2-3 推荐 20 补位至 20 只（D6）

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_recommend_stocks.py:247-285`（`apply_constraints`）、`:371-391`（产物） |
| 做法 | 确认 `apply_constraints(ranked, top=20)` 在候选池足够时仍取满 20；剔除 920599 后由 `backup` 段第一位递补。产物中 `list` 长度断言为 20（若候选不足则按实际并告警） |
| 命令 | `python scripts/_recommend_stocks.py` |
| 验证 | `_推荐20.json`：`len(list)==20`、无 `is_bj` 成员、原 `backup[0]` 已进入 `list`；控制台打印的新 TOP20 需**人工确认** |
| 漂移 | **是**（名单换血）→ commit 注明"口径变更：剔除北交所，备选补位" |

### B2-4 数据/产物清理（清单见优化方向分析附录 C）

| 类型 | 操作 |
| :--- | :--- |
| 个股缓存 8 个 | `git rm` `cache/{股票,分红,财报,股本}_920599.json` 与 `_920509.json` |
| 前端指标 2 个 | `git rm` `web/data/stocks/920599.json`、`920509.json` |
| 汇总/评分产物 | 重跑 `update.py comp → summary → web → rec` 自动剔除 |
| ETF 持仓 | `cache/ETF持仓_159232.json` 中 920599 一行：**保留**（真实持仓，用于 980092 股息率估算；不参与个股池） |
| Excel | 重跑 `update.py excel` 后手工确认 `excel/股票历史.xlsx` 的 `920599 同力股份` sheet 已移除；`excel/红利成分股汇总.xlsx` 对应行移除 |
| 文档 | `红利指数与ETF成分股.md` 6 行（120/519/636/956/1186/1457）**保留**（D1：成分记录保留） |
| 死代码 | `_full_backfill.py:86` 的 `backfill_bj([...])` 删除（该脚本已无引用，建议整体移入 `scripts/_archive/`） |

### B2-5 验收

```bash
python update.py daily --yes      # 日志中不应出现 920599 / 920509
python update.py status           # 无新增失败项
python scripts/_test_analysis.py  # 全绿
```

预期：`manifest.stocks` 364→**362**；`analysis.by_code` 385→**383**；`_成分股汇总表` 329→**327**；`cache/股票_*` 376→**374**；`web/data/stocks/*` 376→**374**；`_推荐20.json.list` 仍为 **20**。

**回滚**：`git revert` B2 全部 commit + `git checkout -- cache/ web/data/`。

---

## B3 · 三主视图瘦身 + 交付层压缩（对应 R3、R4）

### B3-1 `_gen_analysis.py` 补标量字段 + 产出 `dy_series.json`

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_gen_analysis.py:207-211`（by_code 条目）、`:204-206`（dy_series）、`:213`（date） |
| 做法 | ① `by_code[code]` 增加 `dy_now`、`dy_pct`、`dy_p50`、`close_now`；② 新增 `web/data/dy_series.json` = `{date, by_code:{code:[[date,dy],…]}}`，**仅指数+ETF**（`typ in ("指数","ETF")`）；③ `out["date"]` 计算保持不变（此时 analysis_dy 仍含全部 series） |
| 命令 | `python scripts/_gen_analysis.py` |
| 验证 | `analysis.json` 558 KB → 约 580 KB；`dy_series.json` 约 1.7 MB；抽查 600036 四字段与 `analysis_dy` 标量一致 |
| 漂移 | 纯新增字段 |

### B3-2 三视图改读 `analysis.json`

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `web/js/views/scanView.js:12,19,29-39`；`recommendView.js:13,20`；`holdingsView.js:14,24`；`web/js/views/reco.js:38-75`（`buildRecoPool`） |
| 做法 | 删除 `DY_URL` 与 `loadJSON(DY_URL)`；`buildRecoPool(dy, bt, an, m)` 改为 `buildRecoPool(bt, an, m)`，遍历 `an.by_code`；`d.dy_now/d.dy_pct/d.dy_p50/d.close_now` 改取 `an.by_code[code]`；maxDate 改用 `an.date` |
| 验证 | ① `node --check`；② Edge headless：Network 断言**无** `analysis_dy.json` 请求；③ 三视图每行数值与基线逐字段对账（L3 脚本） |
| 漂移 | 无 |

### B3-3 `compareView` 改读 `dy_series.json`

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `web/js/views/compareView.js:243`、`:276-277` |
| 做法 | `loadJSON('/cache/analysis_dy.json')` → `loadJSON('/web/data/dy_series.json')`；`e.series` → `e`（新结构直接是序列数组） |
| 验证 | 对比分析页选中指数/ETF，dy 曲线与改前一致（截图/数值对账） |
| 漂移 | 无 |

### B3-4 `serve.py` gzip + ETag

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `serve.py:52-99`（`NoCacheHandler`） |
| 做法 | ① 覆写 `end_headers`/`send_head`：对 `.json/.js/.css` 且 `Accept-Encoding` 含 gzip 且体积 >1 KB 时压缩并加 `Content-Encoding: gzip` + `Vary: Accept-Encoding`；② 对 `/cache/`、`/web/data/` 计算内容 ETag（size+mtime 或 sha1 前 16 位），命中 `If-None-Match` 返回 304；③ 这两类目录改 `Cache-Control: no-cache`（保留 ETag 校验），`web/js`、`web/css` 保持 `no-store` |
| 命令 | `python serve.py` |
| 验证 | `curl -sI -H "Accept-Encoding: gzip" http://localhost:8000/cache/analysis_dy.json` → `Content-Encoding: gzip`；二次请求 → `304`；改数据重生成后 ETag 变化且刷新拿到新值 |
| 漂移 | 无 |
| 风险 | 缓存误配读旧值 → 只用 ETag 强校验，不设 `max-age` |

**B3 验收**：G2、G3 达成——三视图首屏 31.4/32.3 MB → 0.66 MB；JSON 传输降至 10%–20%。

---

## B4 · R5 全量：落盘体积压缩（5a → 5d，逐步提交）

> **前置**：先执行读取点普查，避免遗漏。
> ```bash
> grep -rn "\.rows\|rows\[\|r\.d\b\|r\.date\|indiUrl\|klineUrl" web/js scripts | grep -v node_modules
> ```
> **已验证前提**：376 个 `web/data/stocks/*.json` 与对应 `cache/股票_*.json` **长度 + 首末日期逐行一致**；且 `historyLayout.js:379` 已有 `ind.length !== rows.length` 兜底，可复用为 5a 的运行时校验。

### B4-1 （5a）指标文件去掉重复日期列

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_gen_web_data.py:240-278`（`build_stock_indicators`）；前端 `historyLayout.js:373-380`、`compareView.js:270-274` |
| 做法 | 落盘行结构 `{"d":…}` → 去掉 `d`（或改为顶层 `dates` 单列一次存储）；前端按**索引**与 K 线行对齐，索引长度不一致时退化为"按日期映射"（保留现有兜底） |
| 命令 | `python update.py web` |
| 验证 | 逐字段对账（改前后 dy/pe/pb/roe/roa/pr 全等）；单只文件体积 −约 26% |
| 漂移 | 格式变更（**破坏性**） |

### B4-2 （5b）列式编码

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `_gen_web_data.py:272`、`_fetch_history.py:67-68`（`save_cache`） |
| 做法 | 写盘格式 `{"cols":[…],"rows":[[…]]}`；读取侧统一走新增的 `_common.decode_rows(obj)`（Python）与 `data.js` 的 `decodeRows(obj)`（前端），**读取点全部改为该函数** |
| 验证 | `cache/股票_600036.json` 926 KB → 约 480 KB；`_test_analysis.py` 全绿；对账脚本 diff 为空 |
| 漂移 | 格式变更（**破坏性**） |

### B4-3 （5c）`roe/roa` 稀疏化

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `_gen_web_data.py:240-278`；前端 `historyLayout.js` 指标曲线构建处 |
| 做法 | `roe/roa` 改为 `[[row_index, value], …]` 变更点数组，前端按索引前向填充（f.fill 语义等价） |
| 验证 | 与改前逐行对账（含填充后逐行相等）；`web/data/stocks` 总积 155 MB → 约 144 MB |
| 漂移 | 格式变更（**破坏性**） |

### B4-4 （5d）`analysis_dy.json` 去掉股票 series

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_gen_analysis.py:476`（写盘前剥离）、`:213`（`out["date"]` 需在剥离前计算或改用 `max_date` 字段）；读取方 `_backtest_analysis.py:35-45`（`merge_close`）、`_backtest_portfolio.py:283-295` |
| 做法 | ① 写盘时 `info.pop("series")`（股票），保留指数/ETF；② `merge_close` 改为从 `web/data/stocks/{code}.json` 取 `dy` 列与 cache close 合并；③ `_backtest_portfolio.py:290,294` 的 `dser` 改取指标文件 `dy` 列 |
| **附带修复** | `_backtest_portfolio.py:290` 目前**在 364 只的循环内**反复 `load_json('cache/analysis_dy.json')`（30.7 MB × 364 ≈ 11 GB 解析量）。5d 顺带消除该开销；若先单独修，可把该行提到循环外 |
| 验证 | `analysis_dy.json` 30.7 MB → 约 **1.8 MB**；`python update.py bt` 与 `python scripts/_backtest_portfolio.py` 产物与基线**逐字段一致** |
| 漂移 | 无（仅存储位置变化） |
| 回滚 | 每步独立 commit，逐步 `git revert` |

**B4 验收**：G4 达成——单只股票传输 ~900 KB → ~100 KB；`analysis_dy.json` → ~1.8 MB。

---

## B5 · 仓库增长治理（对应 R6 ①②）

### B5-1 仅在数据变化时写盘

| 项 | 内容 |
| :--- | :--- |
| 改动点 | `scripts/_fetch_history.py:198-232`（`update_incremental`）、`:262-274`（`get_or_fetch`）、`_fetch_stock_data.py:562-573` |
| 做法 | 合并后与旧 `rows` 比较：**无变化则直接 return，不写盘、不更新 `fetched_at`**；有变化才写 |
| 命令 | 连续两天各跑一次 `python update.py daily --yes` |
| 验证 | 第二个（非交易日/无新数据）运行后 `git status --short` **干净**；`git count-objects -vH` 无增长 |
| 漂移 | 无 |

### B5-2 清理非项目跟踪文件与孤儿缓存

| 类型 | 操作 |
| :--- | :--- |
| 无关文件 | `git rm docs/_zhaomu.pdf docs/_zhaomu.txt`、`git rm -r _cons_tmp/`（先确认无人依赖）；`.gitignore` 追加 `_cons_tmp/` |
| 孤儿缓存 | 12 只（B2 后为 10 只）池外标的：新增 `update.py gc` 或让 `build_stock_indicators` 清理池外指标文件；清理前先输出清单人工确认 |
| 验收 | `git status` 干净；`cache/股票_*` 数量 == `manifest.stocks` 数量 |

**B5 验收**：G5 达成——噪声 commit 归零，月增长目标 <20 MB。

---

## B6 · 单一来源收敛（对应 R7）

| 步骤 | 改动点 | 做法 | 验证 |
| :--- | :--- | :--- | :--- |
| B6-1 | `_common.py` | 新增 `pct_rank(v, arr)`（统一 `x < v`）、`load_json`、`IDX_DIV`、`UA`、`ETF_TRACK`、`StaleDataError` | `python scripts/_common.py` 自测扩展 |
| B6-2 | `_gen_analysis.py:51`、`_recommend_stocks.py:74`、`_backtest_portfolio.py:118` | 三份 `pct_rank` 改 import；**口径统一为 `<`** | 重跑 `update.py web` + `rec`，快照刷新并注明漂移 |
| B6-3 | 前缀 10 处（`_fetch_stock_data.py:69,136,167,194` 等） | 全部改 `_common.market_prefix` | `_test_analysis.py` 全绿 |
| B6-4 | `update.py:35-46,60-67,381-396…` | 改调 `_common.atomic_load/atomic_dump/safe_export`；抽 `rebuild_web(with_rec=True)` 消除 6 份复制 | `python update.py status` 正常 |
| B6-5 | `_fetch_etf_holdings.py:21,160`、`_gen_analysis.py:303`、`_gen_web_data.py:348` | 清退役 `159229` 与误导注释 | 产物不变 |
| B6-6 | 死代码 | `_fetch_index_data.py`、`_find_index_code.py`、`_recommend_data.py`、`_candidate_dy.py`、`_full_backfill.py`、`_gen_summary_excel.py` → `scripts/_archive/` + README 说明（**先确认无人手动使用**） | 全链路命令仍可跑 |

**验收**：G6 达成——各实现唯一一份；`pct_rank` 统一后分数微调属预期漂移。

---

## B7 · 工程化护栏（对应 R8）

| 步骤 | 改动点 | 做法 | 验证 |
| :--- | :--- | :--- | :--- |
| B7-1 | `.github/workflows/daily-update.yml`（回归步骤） | 增加 `python scripts/_common.py`（离线 0.1 s） | CI 绿 |
| B7-2 | `scripts/_test_analysis.py` | 新增 3 条关系断言：`len(推荐20 分组) == len(_推荐20.json.list)`、TOP20 因子覆盖率 ≥7/10、`manifest.stocks` 与 `cache/股票_*.json` 一一对应 | 人为造回归 → CI 红 |
| B7-3 | 新增 `pyproject.toml` | `[tool.ruff] select = ["F","E9","B"]` | `ruff check .` 首次报出 `_fetch_stock_data.py:20` 未用 `random`、`_backtest_analysis.py:13` 未用 `io` |
| B7-4 | 新增 `tests/frontend/` | `node --test` 覆盖 `web/js/views/analysis.js`、`reco.js` 纯函数（`scoreOf` / `buildRecoPool` / `recoBandOf`） | `node --test` 通过 |

**验收**：G7 达成——新增回归能在 CI 变红。

---

## B8 · 回测执行日对比报告（对应 R9）

| 步骤 | 内容 |
| :--- | :--- |
| B8-1 | `scripts/_backtest_analysis.py:63-64,73`：新增 `--exec-offset {1,2}` 开关（**默认 1，不改默认口径**），执行日索引与收益起点同步偏移 |
| B8-2 | 以 `--exec-offset 2` 重跑 `python update.py bt` + `python scripts/_backtest_portfolio.py`，产出 `docs/回测执行日对比.md`（指数 / ETF / 推荐20 的 1/3/6/12M 超额与胜率对比） |
| B8-3 | **决策门**：根据报告判断是否将默认口径改为 +2（若改：独立 commit + 全量产物重跑 + 结论刷新 + 在 `docs/回测报告.md` 注明口径） |
| 验证 | 报告给出"12M 超额 +24% → ?"及是否反转；默认口径产物在 B8-1/2 期间**保持不变** |

---

## 附录 A · 验证命令总表

```bash
# L1 语法
python scripts/_common.py
cp web/js/views/reco.js /tmp/reco.mjs && node --check /tmp/reco.mjs

# L2 单测
python scripts/_test_analysis.py            # 60 项
python scripts/_test_analysis.py --no-snapshot

# L3 对账（B3/B4/B6 前后各跑一次）
python - <<'PY'
import json, sys
for p in ["web/data/analysis.json","web/data/backtest.json","web/data/manifest.json","cache/analysis_dy.json"]:
    a=json.load(open(".baseline/"+p.split("/")[-1],encoding="utf-8"))
    b=json.load(open(p,encoding="utf-8"))
    print(p, "IDENTICAL" if a==b else "DIFF")
PY

# L4 端到端
python update.py daily --yes
python update.py status

# L5 浏览器
python serve.py   # 另开窗口
# Edge headless: Network 断言无 /cache/analysis_dy.json 请求；getOption() 校验 legend/series

# L6 演练
# 截断 cache/股票_600036.json 的 rows → 验证 B1 告警 → git checkout -- cache/
```

## 附录 B · 提交计划

| # | commit 主题（中文 + 要点分列） |
| :--- | :--- |
| 1 | 抓取空返回判失败 + status 每组区间 + manifest 陈旧标记：... |
| 2 | 剔除北交所标的（个股池，成分记录保留）+ 备选补位至 20：... |
| 3 | 三主视图改读 analysis.json + 新增 dy_series.json：首屏 31MB→0.66MB |
| 4 | serve.py 启用 gzip + ETag（数据文件可缓存、代码仍 no-store） |
| 5 | 指标文件去重复日期列（R5a） |
| 6 | 列式编码 + 统一 decode（R5b） |
| 7 | roe/roa 稀疏化（R5c） |
| 8 | analysis_dy 去股票 series + 后端改读指标 dy 列（R5d） |
| 9 | 仅在数据变化时写盘 + 清理无关跟踪文件与孤儿缓存 |
| 10 | 单一来源收敛（pct_rank/前缀/常量/ETF 元数据）+ 死代码归档 |
| 11 | CI 护栏（_common 自测 + 3 条关系断言 + ruff + node --test） |
| 12 | 回测执行日对比报告（--exec-offset，默认口径不变） |

## 附录 C · 回滚手册

| 场景 | 操作 |
| :--- | :--- |
| 单步回滚 | `git revert <commit>` |
| 数据回滚 | `git checkout -- cache/ web/data/` |
| 产物回滚到基线 | 从 `.baseline/` 覆盖 `web/data/` |
| 整批回滚 | `git reset --hard <基线 commit>`（**先确认无未提交工作**） |

## 附录 D · 完成标准（DoD）

1. G1–G7 全部达成（§1 目标表逐项打勾）。
2. `python scripts/_test_analysis.py` 全绿（快照漂移已注明）。
3. `python update.py daily --yes` 后 `git status --short` 在**无新数据时干净**。
4. 三视图 Network 无 `analysis_dy.json` 请求；首屏 < 1 MB。
5. `_推荐20.json` 为 20 只且不含北交所；回测"推荐20"分组数 == list 长度。
6. `docs/回测执行日对比.md` 已产出，默认口径决策已记录。

---

*本文档为执行方案，未修改任何代码或数据。*
