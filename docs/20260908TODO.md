# 2026-09-08 TODO（后续优化执行顺序）

> **依据**：`docs/20260908执行方案.md`（B1–B8）与 `docs/20260908需求设计方案.md`（R1–R9，D1–D6 已确认）
> **粒度**：功能点级（不展开到代码行；实现细节见执行方案）
> **用法**：按 T 编号顺序推进；每项完成即勾选，并在右侧记录 commit。**未确认前不改代码。**
> **状态标记**：`[ ]` 待办 · `[~]` 进行中 · `[x]` 完成 · `[!]` 阻塞

---

## 总览

**进度**：**T1–T32 全部完成**（2026-09-08 期）· 下一期清单见文末「下一期 TODO（N1–N20）」

| 阶段 | 主题 | 功能点 | 优先级 | 依赖 |
| :--- | :--- | :--- | :--- | :--- |
| 一 | 数据可信（陈旧可见） | T1–T4 | P0 | — |
| 二 | 北交所剔除 | T5–T9 | P0 | 阶段一 |
| 三 | 首屏瘦身与传输压缩 | T10–T15 | P0 | — |
| 四 | 落盘体积压缩 | T16–T19 | P1 | 阶段三 |
| 五 | 仓库增长治理 | T20–T21 | P1 | 阶段二 |
| 六 | 单一来源收敛 | T22–T25 | P1 | 阶段二 |
| 七 | 工程化护栏 | T26–T29 | P1 | 阶段一–六 |
| 八 | 回测研究有效性 | T30–T32 | P0 | 阶段四 |

---

## 阶段一 · 数据可信（P0）

- [x] **T1 抓取空返回判失败** — 抓取返回空且落后基准 >3 交易日 → 入陈旧报告，不再"每天成功、新增 0 条"
  - 验收 ✅ 2026-09-08：`_common.find_stale()` + `update_stale_report()`；三池（推荐20/其他成份股/自选股）写入 `cache/_stale.json`
  - 验证：drill 走真实 `main()` 循环（600036=09-08 / 920599=08-24 / 000858=09-05）→ 仅命中 920599（15 天）；`_common.py` 自测 23 项 PASS；`_test_analysis.py --no-snapshot` 60/60 PASS
  - 实现说明：基准取**本轮全池最大 last_date**（自包含，不引入交易日历）；与"抓取失败清单"分离，语义不混用
- [x] **T2 status 分组区间 + 滞后清单** — 每组显示 min/中位/max 日期，并列出滞后 >2 交易日的 code
  - 验收 ✅ 2026-09-08：每组输出 `min ~ max（中位 …，N 只）` + 滞后清单（阈值 3 自然日≈2 交易日）
  - 实测（真实数据即命中）：`推荐20: 2026-08-24 ~ 2026-09-08（中位 2026-09-08，20 只）⚠️ 滞后 1 只` → `920599 同力股份 截至 2026-08-24，落后 15 天`（旧版每组只抽查 1 只，完全看不见）
  - 另读 T1 的 `cache/_stale.json` 输出"抓取陈旧"摘要行；`_test_analysis.py --no-snapshot` 60/60 PASS
- [x] **T3 manifest 陈旧标记** — 每标的增加 `ind_last` / `stale`，顶层加 `stale_n`
  - 验收 ✅ 2026-09-08：364/364 标的均有 `ind_last`；`stale` 仅 920599 为 true；`stale_n=1`；`update.py web` 打印「⚠️ 数据陈旧 1 只（指标末日期落后 ≥3 自然日）：920599 同力股份（2026-08-24）」
  - 实现说明：`ind_last` 复用 build_manifest 已解析的指标文件末行（**零额外 IO**）；阈值 `STALE_LAG_DAYS=3` 收进 `_common.py`，与 T2 的 status 清单共用
  - 未改 `data_date` 口径：它取全池 max 且与 `analysis.date` 严格一致（回归有断言）；单只滞后已由 per-标的 `stale` 暴露，改基准反会引入不一致告警
- [x] **T4 前端陈旧角标** — K 线页标的标题旁显示"数据滞后（截至 YYYY-MM-DD）"
  - 验收 ✅ 2026-09-08（静态）：`stockView.js:74` 补传 `manifestMap`（**原缺失——renderMain 里 `m` 恒为 `{}`，角标永远不会出现**）；`historyLayout.js:405/468` 读 `m.stale`/`m.ind_last` 渲染角标 + tooltip；`style.css:346` 加 `.stale-badge`（与 `.ticker-scale` 同款 chip）
  - 数据契约校验（node 跑真实 manifest）：920599 → 「数据滞后 2026-08-24」；默认首只 600919 → 无角标；`stale_n=1`
  - ✅ 浏览器实测（2026-09-09 沙箱放开后补验）：`tests/browser/smoke.mjs`（playwright-core + Edge headless）注入 stale → 角标文本「数据滞后 2026-08-24」渲染成功、K线图 15 series、无 JS 异常（详见下方 T12/T13 同批 15 项）
  - 人工核对：`python serve.py` → `#/stock` → 推荐列表点 **920599** → 标题旁应见琥珀色「数据滞后 2026-08-24」

## 阶段二 · 北交所剔除（P0）

- [x] **T5 北交所唯一判定** — `_common.is_bj()` 单一实现 + 自测用例
  - 验收 ✅ 2026-09-08：`is_bj(code) = code.startswith(("43","83","87","88","92"))`，置于 `market_prefix` 之后并注明**语义差异勿互替**（market_prefix 是行情路由，400xxx 老三板也走 bj；is_bj 只回答"是否北交所个股"）
  - 自测：23 → **36 项全绿**（+6 True：920599/920509/430047/830799/870436/889999；+8 False：600036/000858/300750/688111/510880/159201/900901/400123，其中 900901 守住"9 号段≠92"边界）
  - 真实池扫描（376 缓存 / 364 manifest / 329 汇总表）：is_bj 命中**恰好 920599 + 920509**，零误伤
  - 基线校正：`analysis.by_code`=385（11 指数 + 12 ETF + **362 股票**）**已不含北交所**——二者因历史 <60 日被 `enough_history` 过滤，故 T6 后该数不变（非 383）
- [x] **T6 个股池全线排除北交所** — 评分候选 / 抓取 / 前端列表 / 回测分组 / 持仓可选项（6 处池构造点）
  - 验收（静态，2026-09-08）：8 处改动——`_recommend_stocks.py` 硬过滤新增分支 `北交所(不纳入)`（**权威点**）、`_fetch_stock_data._rec_stocks()`、`_fetch_pool_data.pool_codes()`、`_gen_web_data.stock_pool()`、`_gen_analysis.emit()`、`_update_summary` 抓取侧 `codes` + `build_table()`、`_fetch_watchlist.main()` 护栏
  - 纯池扫描（不触网）：`fsd.STOCKS` 19 只无北交所（T7 补位回 20）、`pool_codes` 309 无、`stock_pool` **362** 无、汇总表待写 **327**/329、watchlist 51 无；`_recommend_stocks.py --no-write` 实测打印 `✗ 920599 同力股份: 北交所(不纳入)`
  - 前端 D1：`indexView`/`etfView` 传 `stockCodes`（manifest 股票集）→ `historyLayout` 成分表无 manifest 条目的行渲染为纯文本 + tooltip「无行情数据、不参与计算」；模拟 BJ-free manifest 后 5 张成分表（000922/H30270/932315/515180/159209）恰好 920599/920509 不可点开
  - 顺延：产物重生成（manifest 362 / 汇总表 327 / 推荐20 补位至 20）在 **T7/T8**；`python update.py daily --yes` 全量跑留到阶段二自检（避免现在跑产生 fetched_at 脏改动，见 T20）
  - 说明：两处 `_FALLBACK_*` 硬编码清单本就无北交所（无需删）；`_update_summary` 抓取侧过滤额外满足 R2②"不再发起任何抓取请求"
- [x] **T7 推荐 20 备选补位** — 剔除 920599 后由备选补位，`list` 保持 20 只（D6）
  - 验收 ✅ 2026-09-08：`_推荐20.json` list **20 只且无北交所**；新入榜 **600582 天地科技**（机械设备/周期，均衡 61.0，dy 6.3%），原 920599 出榜
  - 机制：`apply_constraints` 第二轮从 `ranked` 按总分补满；920599 退出后"象限保底 3 只"的周期象限空出一席 → 600582 递补并排第 20
  - ⚠️ 与执行方案 B2-3 预期**不同**：`backup[0]`=601398 工商银行**未**递补——一级行业"银行"已满 4 只（行业≤4 优先于补位顺序），backup 前 10 位全为银行股；若要 601398 入榜须放宽行业上限（属独立口径决策，未做）
  - 护栏：候选不足 top 时打印告警；产物打印改用实际 `len(picked)/len(backup)`（原打印的是请求值 top，候选不足会显示失真）
  - 验证：20 只成员 manifest 条目 + 指标文件 + K线缓存齐备；行业计数最高 4（银行）合规；`fsd.STOCKS`=20 含 600582；`FINAL20`=20；`stock_pool()`=**362**
  - 漂移：**是**（名单换血 920599→600582；`excluded` 157→158，两北交所标的均记「北交所(不纳入)」）；下游产物（manifest rec 标记 / 回测分组 / 汇总表）待 T8 重生成
  - 待人工确认：新 TOP20 名单
- [x] **T8 北交所缓存与产物清理** — 8 个个股缓存 + 2 个前端指标 + Excel sheet + 汇总产物
  - 验收 ✅ 2026-09-08：`git rm` 10 个文件（`cache/{股票,分红,财报,股本}_920599|920509.json` + `web/data/stocks/{920599,920509}.json`）
  - 产物重生成后：`manifest.stocks` **362** / `_成分股汇总表.json`·`summary.json` **327** / `cache/股票_*` **374** / `web/data/stocks` **374** / `analysis.by_code` **385 不变** / `_推荐20.json.list` **20** / `manifest.stale_n` **0**（唯一陈旧项就是 920599）
  - 附带清理：`cache/_自选股指标.json` 剔除 **333** 条北交所（5537→5204）；`_fetch_watchlist.refresh_indicators_main()` 加北交所跳过 —— **T6 遗漏点**（指标刷新遍历 xlsx 全量行，含 展示=0 的北交所行）
  - 兜底：`_gen_web_data.build_manifest()` 的 `all_codes` 加 `is_bj` 过滤（该函数自建代码表，不经过 `stock_pool()`）
  - 保留（D1）：`components.json` 北交所成分 **7 行**、`cache/ETF持仓_159232.json` 920599 一行、`红利指数与ETF成分股.md`、`excel/自选股清单.xlsx` 两行（用户维护，展示=0，**未改动**）
  - 重生成范围（纯计算，未触发无关网络拉取）：汇总表 + 红利成分股汇总 Excel、manifest/components/summary、回测报告 + backtest.json、股票历史 Excel
  - 回测分组漂移：推荐20 **17→18**（600582 有信号样本；920599 原本无样本）、其他成份股 **248→247**（920509 出池）；推荐20 组 12M 超额 +17.30%→+16.89%
  - 格式修正：`build_table` 改紧凑写（与 `web/data/summary.json` 同款）——否则该文件产生 **1.7 万行纯格式 diff / +74KB**
  - 其他漂移：`cache/股票_{600064,600096,600582}.json` 末几行补 `chg30/60/90`（`export_excel` 既有副作用）
  - 自检：`update.py status` → 推荐20 20 只 / 其他成份 294 只 / 自选 51 只，均 2026-09-08；`_test_analysis.py --no-snapshot` **60/60 PASS**
- [x] **T9 死代码归档** — `_full_backfill.py` 等 6 个无引用脚本移入 `scripts/_archive/`，清北交所硬编码
  - 前置证据 ✅：全仓扫描 `update.py`/`serve.py`/`scripts/**/*.py` 对 6 个脚本**零引用**（仅 README/AGENTS/文档说明性提及）；`scripts/` 顶层 .py 23→17
  - 归档清单：`_fetch_index_data` / `_find_index_code` / `_recommend_data` / `_candidate_dy` / `_full_backfill` / `_gen_summary_excel`（后者与 `_update_summary.gen_excel` 重复）
  - 清北交所硬编码：`_full_backfill.py` 删除 `backfill_bj()` 及其调用（原硬编码 920599/920509）
  - 新增 `scripts/_archive/README.md`：归档原因/清单/注意事项（相对路径须在项目根运行）/恢复方法；README.md 与 AGENTS.md 脚本索引同步指向 `_archive`
  - 验证：6 个归档脚本 `py_compile` 全通过；代码残留引用 0；`_test_analysis.py --no-snapshot` 60/60 PASS；无数据漂移
  - 阶段二自检 ✅ 2026-09-08：`python update.py daily --yes`（827 秒）→ 日志 3800+ 行**仅 1 处**北交所提及（`wq` 护栏提示，333 只 xlsx 北交所行按 R2 跳过），**无 920599/920509 请求**；`_test_analysis.py --no-snapshot` 60/60 PASS
  - 自检发现并修复 **T1 缺口**：`update.py:update_stocks()` 自带循环未做陈旧检测 → 「推荐20」池一直缺席 `cache/_stale.json`（报告只写在 `_fetch_stock_data.update_all()`，而 daily 走的是 update.py 的重复实现）；补 `records/find_stale/update_stale_report`，现三池齐全
  - 自检数据漂移：manifest 仅 ETF `scale` 8 只刷新（股票字段 0 变化）；`_推荐20.json`（excluded 158→156）、`_自选股指标.json`、`_ETF规模.json`、`_change_log.json`；5 个 Excel 重生成（含用户 `excel/自选股清单.xlsx` 指标列写回）；新增 `cache/_stale.json`

## 阶段三 · 首屏瘦身与传输压缩（P0）

- [x] **T10 analysis.json 补标量字段** — `by_code` 增加 `dy_now / dy_pct / dy_p50 / close_now`
  - 验收 ✅ 2026-09-08：385/385 标的 **5 字段齐备**（含额外补的 `dy0`）且与 `cache/analysis_dy.json` 标量**逐值一致**（容差 1e-9）
  - 核实：`build_factors()` 原只写 factors/anchors/分位——四标量仅存于 30.7MB 的 `cache/analysis_dy.json`，故三主视图首屏被迫拉全量序列（方案前提成立）
  - 额外补 `dy0`：`scanView.js:31` / `reco.js:50` 以 `d.dy0 == null` 作无数据判据，带上才能让 T12 原样替换
  - 口径不变：`dy_pct` 仍为原始股息率分位（高=便宜）；`factors.dy.pct` 仍为 `100-dy_pct`；ETF 的 `close_now` 仍是跟踪指数点位（真实价取 manifest.last_close）
  - 体积：analysis.json 558→**588.5 KB**；`cache/analysis_dy.json` 字节未变（重算确定性）；`_test_analysis.py --no-snapshot` 60/60 PASS；无数据漂移
- [x] **T11 产出 dy_series.json** — 仅指数 + ETF 的 dy 序列（约 1.7 MB）
  - 验收 ✅ 2026-09-08：`web/data/dy_series.json` = `{date, by_code:{code:[[date,dy],…]}}`，**23 标的**（11 指数 + 12 ETF），**1.80 MB**
  - 键集 == `analysis_dy` 中 `type∈(指数,ETF)` 全集（差集为空，无北交所）；每条序列与 `analysis_dy` **逐点一致**（含 000922 4400 点，首 2008-07-31 / 末 2026-09-08 dy 4.2221 == by_code.dy0）
  - 取**全量**序列而非近 1250 点截断：compareView 现按 `e.series` 全量对齐（`compareView.js:276-277`），截断会丢 5 年以前区间（截断后仅 0.60 MB，但属功能倒退）
  - `analysis.json` 字节未变；`_test_analysis.py --no-snapshot` 60/60 PASS
- [x] **T12 三主视图改读 analysis.json** — 信号扫描 / 智能推荐 / 我的持仓去掉 30.7 MB 的 `analysis_dy.json` 请求
  - 验收 ✅ 2026-09-08（数据契约校验，node 全量 385 标的）：`buildRecoPool` 候选池 **322 == 322**、字段逐项**零差异**、`maxDate` 一致；`scanView` 全池 **385 行逐行一致**、`maxDate` 一致；`holdingsView` 用到的 `name/type/dy_pct/dy_now/close_now` 全量比对 **0 处不一致**
  - 改动：`scanView` 遍历 `analysis.by_code` + 「数据日期」改用 `analysis.date`；`reco.buildRecoPool(an, bt, m)` 改从 `by_code` 取数、`maxDate` 改按**池内 `manifest.last` 取最大**（原按 series 末日期，口径等价——`by_code` 不含 series）；`recommendView`/`holdingsView` 去掉 `DY_URL` 并改调新签名；`holdingsView` 的 `dy[p.code]` → `byCode[p.code]`（含 273/467 两处名称回退）
  - 验证：ESM `node --check` 4/4 通过；`_test_analysis.py --no-snapshot` 60/60 PASS；`compareView` 仍读 `analysis_dy`（**T13** 处理）
  - ✅ 浏览器实测（2026-09-09 补验）：`#/scan`/`#/recommend`/`#/holdings` 各 **0 次** `analysis_dy.json` 请求、均读到 `analysis.json`、无错误框（`tests/browser/smoke.mjs` 15 项 PASS）
- [x] **T13 compareView 改读 dy_series.json** — 对比分析页 dy 曲线数据源切换
  - 验收 ✅ 2026-09-08（node 全量）：对比页 23 个可选标的（11 指数 + 12 ETF）在 `dy_series.by_code` 中**全部存在**；`dyPts` 与旧源（`analysis_dy[code].series`）**逐点一致**（不一致 0）；抽样 000922 4400 点末 2026-09-08=4.2221、000015 5267 点、000821 3356 点
  - 改动：`data.js` 新增 `DY_SERIES_URL`；`compareView` 改读该文件，`dyPts` 直接取 `by_code[code]`（原需再取 `.series`）
  - 全仓复查：`web/js` 中**已无** `/cache/analysis_dy.json` 引用（仅注释提及）→ 前端彻底不请求 30.7MB 文件；ESM `node --check` 2/2；`_test_analysis.py --no-snapshot` 60/60 PASS
  - ✅ 浏览器实测（2026-09-09 补验）：对比页选中 000922/000015 → 请求 `dy_series.json` **1 次**、`analysis_dy.json` **0 次**、图表 2 条系列（`tests/browser/smoke.mjs`）
  - 后续：`analysis_dy.json` 现仅后端使用（`_gen_analysis` 中间产物 + `_backtest_portfolio.py:290` 循环解析），由 **T19/R5d** 收缩
- [x] **T14 serve.py gzip** — `.json/.js/.css` 启用 gzip（>1 KB 才压）
  - 验收 ✅ 2026-09-08（真实 HTTP，本地 `serve.py 8123` + python urllib）：`Content-Encoding: gzip` 命中，实测压缩率
    · `analysis.json` 602,622 → **116,196**（-80.7%）· `web/data/stocks/600004.json` 615,549 → **76,009**（-87.7%）
    · `charts.js` 38,522 → 11,234 · `fonts-local.css` 1,032 → 193（刚好过 1KB 阈值）
  - 边界用例：<1KB 的 js(144B)、`favicon.svg`、`index.html` **均不压缩**；不带 `Accept-Encoding` 不压缩；解压后字节数与磁盘**逐一相等**
  - 回归：404 / 目录列表 / HEAD 行为不变；`_test_analysis.py --no-snapshot` 60/60 PASS
  - 实现：`NoCacheHandler` 覆写 `send_head()`（文本类 + ≥1KB + 请求头含 gzip → 压缩并改写 `Content-Length`、加 `Content-Encoding`/`Vary`）；目录/索引/错误响应仍交回标准实现
- [x] **T15 serve.py ETag/304 + 数据文件缓存策略** — `/cache`、`/web/data` 改 `no-cache` + ETag；JS/CSS 保持 `no-store`
  - 验收 ✅ 2026-09-08（真实 HTTP，本地 `serve.py 8124` + python urllib）：
    · 数据文件 `manifest.json` → 200 + `ETag: "6aa0255b-1c0d3-gz"` + `Cache-Control: no-cache` + gzip；带 `If-None-Match` 二次请求 → **304 空响应体**（ETag 回显、无 Content-Length）
    · `/cache/股票_600036.json` 同样 `no-cache` + ETag + gzip；`/web/js/app.js` → `no-store, no-cache, must-revalidate` 且**无 ETag**
    · 数据更新 → ETag 变（`6aa02b92-a` → `6aa02b93-13`）→ 200 拿到**新值**；再校验 → 304
    · 回归：不带 `Accept-Encoding` 不压缩且 ETag 无 `-gz` 后缀；404 / 目录列表不变
  - 实现：ETag 由 `mtime+size` 派生（重生成即变）+ 编码后缀（同一 ETag 不对应两种表示）；`_cache_control()` 按路径前缀区分数据/代码；`Vary: Accept-Encoding` 对所有可压缩类型统一发送
  - `_test_analysis.py --no-snapshot` 60/60 PASS

## 阶段四 · 落盘体积压缩（P1）

- [x] **T16 指标文件去重复日期列（5a）** — 按索引与 K 线对齐（已验 376/376 逐行一致）
  - 验收 ✅ 2026-09-08（逐字段对账 **374/374**）：字段不一致 **0**；K线日期逐行对齐异常 **0**；`compareView` dyPts 全 374 只**逐点一致**；体积 **154.8 MB → 131.2 MB（−15.3%）**
  - 结构：`[{d,dy,pe_ttm,…}]` → `{last, rows:[{dy,pe_ttm,…}]}`——日期列不落盘，仅保留末日期 `last`（**T3 的 `ind_last` 陈旧检测需要它**，故未裸删日期）
  - 读取点同步 11 处：Python `_gen_web_data.build_manifest`/`build_stock_indicators`、`_gen_analysis.build_stock`/`build_stock_factors`、`_backtest_portfolio`（t 日索引改取同索引 K 线日期）、`_recommend_stocks`、`_test_analysis`×2；JS `historyLayout.js`（解包 `.rows`）、`compareView.js`（股票 dyPts 按索引配 K 线日期）
  - 回归：`analysis.json`/`analysis_dy.json`/`dy_series.json` **字节未变**；`_test_analysis.py --no-snapshot` 60/60 PASS；`_backtest_portfolio.py` 重跑通过
  - 偏差说明：方案预估 −26%，实测 **−15.3%**（日期列约占单行 18/113 字节）；另 12 个池外孤儿指标文件同步转格式（T21 清理）
  - 数据漂移：组合回测产物重生成（上次 2026-09-07 → 含一日行情推进 + T7/T8 池变更，与本次格式改动无关）
- [x] **T17 列式编码 + 统一解码（5b）** — `{cols, rows}` + `decode_rows`/`decodeRows`
  - 验收 ✅ 2026-09-08（全量对账）：**1528** 个 cache 文件逐行逐字段 **0 处不一致**；**cache 196.4 MB → 101.0 MB（−48.6%）**；374 指标文件 JS `decodeRows` 与 Python `decode_rows` 输出**完全一致**（抽样 4 文件逐行相等）
  - 实现：`_common.encode_rows/decode_rows`（列序=首次出现并集，缺失位 None）；`_fetch_history.save_cache`/`load_cache` 收口编解码；指标文件写 `{last, cols, rows}`；`data.js` 新增 `decodeRows`
  - 读取点：Python 全部走 `fh.load_cache`/`decode_rows` —— 含此前**绕过 load_cache 的 6 处** `json.load`（`_gen_analysis`×3、`_fetch_stock_data`×2、`_backtest_analysis`×1），并删除 `_gen_analysis` 自带的重复 `load_cache`；JS 4 处（`historyLayout.loadTickerObj`、`compareView` 的 K线/分红/指标、`holdingsView` 分红）
  - ⚠️ 语义要点：列式解码**按 cols 补齐全部键**（缺失位 None）——必须补齐，否则 `fins[i]["bps"]` 这类直接下标取用会 KeyError（迁移时实测踩到）；因此 9 处 `"close" in r` 判定已改为 `r.get("close") is not None`
  - 回归：`analysis.json`/`analysis_dy.json`/`dy_series.json`/`backtest.json`/`回测报告.md` **字节未变**；`_common` 自测 42 项 PASS；`_test_analysis.py --no-snapshot` 60/60 PASS；`_gen_analysis`/`_recommend_stocks --no-write`/`_backtest_analysis` 全通过
  - 漂移：无（纯编码变更，值逐字段一致）
- [x] **T18 roe/roa 稀疏化（5c）** — 改变更点数组，前端前向填充
  - 验收 ✅ 2026-09-09（全量对账 **374/374**）：逐行逐字段（含填充后 `roe/roa`）不一致 **0**；`web/data/stocks` **60.5 MB → 46.8 MB（−22.6%）**
  - 依据：roe/roa 是阶梯型——600036 的 5438 行里 **roe 只变 83 次、roa 只变 86 次**
  - 结构：指标文件 `{last, cols, rows, sparse:{roe:[[i,v],…], roa:[…]}}`（roe/roa 移出 cols）；`_common` 新增 `encode_sparse/decode_sparse/decode_indicator`；Python 7 处读取改走 `decode_indicator`，前端 `data.js` 新增 `decodeIndicator`（`historyLayout`/`compareView` 各 1 处）
  - 回归：JS `decodeIndicator` 与 Python `decode_indicator` 输出**完全一致**（抽样 3 文件逐行相等）；`analysis.json`/`analysis_dy.json`/`dy_series.json` **字节未变**；`_common` 自测 **46 项 PASS**；`_test_analysis.py --no-snapshot` 60/60 PASS；三条管线全通过
  - ⚠️ 方案基线校正：5c 验收写「155 MB → 约 144 MB」，但该基线已被 T16/T17 降到 60.5 MB，故按**实测**口径记录（−22.6%）
  - 漂移：无（纯编码变更，值逐字段一致）
- [x] **T19 analysis_dy 去股票 series（5d）** — 后端改读指标文件 dy 列；30.7 MB → ~1.8 MB
  - 验收 ✅ 2026-09-09：`cache/analysis_dy.json` **30.74 MB → 1.91 MB**（股票 series 占 26.20 MB）；385 标的中仅 23 只（指数+ETF）保留 series
  - 做法：写盘前 `pop("series")` 并留 `last_date`（供 `analysis.date` 计算）；`_backtest_analysis.stock_dy_series()` 从指标文件 dy 列按索引与 K 线对齐取序列（dy>0 与旧口径一致）；`_test_analysis.series_of()` 统一取序列（带缓存）
  - 附带修复：`_backtest_portfolio.py` 原在 362 只循环内反复解析 30.7 MB 的 `analysis_dy.json`（**单次 0.51 s × 362 ≈ 186 s**）→ 改为直接用指标文件 dy 列，整轮耗时降到 **23 s**
  - 回归：`web/data/analysis.json`、`dy_series.json` **字节未变**；`backtest.json`/`回测报告.md`/`portfolio_backtest.json`/`组合回测报告.md` 与基线**逐字段一致**（仅「生成日期」随系统时钟 09-08→09-09，已还原）；`_test_analysis.py --no-snapshot` 60/60 PASS
  - 漂移：无（仅存储位置变化）
  - 附带：消除 `_backtest_portfolio.py:290` 循环内重复解析 30.7 MB 的开销
  - 验收：`update.py bt` 与组合回测产物与基线逐字段一致

## 阶段五 · 仓库增长治理（P1）

- [x] **T20 仅数据变化时写盘** — 无新增数据时不写盘、不更新 `fetched_at`
  - 验收 ✅ 2026-09-09：连续三次 `python update.py daily --yes`（792s / 777s / 785s）；**第三次（无新数据）后 `git status --short` 仅剩本次代码改动，数据与产物零变更**；`git count-objects` 无增长
  - 写盘收口：`_fetch_history.save_cache_if_changed()`（行数据一致则不写、`fetched_at` 不变）→ `update_incremental`/`get_or_fetch`/股票全量刷新/分红·财报·股本/`export_excel` 写回全部改走它
  - 日期型产物：`_common.atomic_dump_if_changed(ignore=("date",…))`（JSON 归一后比较，避免内存 tuple vs 磁盘 list 误判）→ `成分_980092_股息率.json`、`_推荐20.json`；`_ETF规模.json` 按规模数值比较；`_change_log.json` 两代均已最新则不写
  - Excel：`_common` 新增 `_xlsx_same/replace_if_changed/save_workbook_if_changed`（忽略 `docProps/core.xml` 时间戳）→ `export_workbook` 与 4 处直接 openpyxl 写盘改为「内容未变不覆盖」，用户 `自选股清单.xlsx` 亦不再无谓重写
  - 指数增量合并保留 `chg30/60/90` 列（原丢弃 → 每次 daily 写盘两次）；`cache/_stale.json` 改为**不跟踪**（运行时报告含 `checked_at`，已入 `.gitignore`，磁盘文件仍在）
  - 漂移：无
- [x] **T21 清理无关跟踪文件与孤儿缓存** — `_zhaomu.pdf/.txt`、`_cons_tmp/`、池外孤儿标的
  - 验收 ✅ 2026-09-09：`cache/股票_*` **362 == manifest.stocks 362**；`web/data/stocks` 362；`分红/财报/股本` 无主残留 0
  - 孤儿缓存：新增 `python update.py gc`（**默认只列清单**，`--yes` 才删）→ 实测 12 只 / 60 文件 / **3.62 MB** 已清理（`001386 002001 002602 002773 300002 300972 600031 600131 600801 603816 603885 688599`，均不在任何池内）
  - `_cons_tmp/`（18 个 xls，`_gen_components.py` 的下载工作目录）：`git rm --cached` 取消跟踪 + `.gitignore`，**磁盘文件保留**（脚本仍可运行）
  - `docs/_zhaomu.pdf`（1.27 MB）+ `docs/_zhaomu.txt`（280 KB）：无任何脚本/文档引用（仅规划文档提及）→ `git rm`；如需恢复：`git show <本次commit>^:docs/_zhaomu.pdf > docs/_zhaomu.pdf`
  - 回归：`_test_analysis.py --no-snapshot` 60/60 PASS；`git status` 干净

## 阶段六 · 单一来源收敛（P1）

- [x] **T22 pct_rank 单一来源（统一 `<`）** — 三份合一，分数微调属预期漂移
  - 验收 ✅ 2026-09-09：`_common.pct_rank()` 唯一实现（**严格小于**，D3）；删除 `_gen_analysis`/`_recommend_stocks`/`_backtest_portfolio` 三份本地实现（原为 1 份 `<` + 2 份 `<=`）
  - 实测漂移（符合预期）：`_推荐20.json` **名单与备选完全不变**（20 只同序），10/20 只分数 **+0.1**；组合回测少数季度收益 **±0.1~0.2pp**；`analysis.json`/`analysis_dy.json`/`dy_series.json`/`backtest.json` **字节未变**（`_gen_analysis` 原本就是 `<`）
  - 实现注意：`pct_rank` 的判空必须用 `len(arr) == 0`——`_gen_analysis` 会传 numpy 数组，`not arr` 会抛「truth value of an array is ambiguous」（首轮实测踩到并修正）
  - `_common` 自测 **50 项 PASS**；`_test_analysis.py --no-snapshot` 60/60 PASS
- [x] **T23 前缀 / 常量 / ETF 元数据收敛** — 前缀 10 处、`IDX_DIV`、`UA`、`WINDOW`、ETF 元数据 5 份 → `_common`
  - 验收 ✅ 2026-09-09：`scripts/` 内**已无**本地 `UA =` / `WINDOW =` / `IDX_DIV =` / `ETF_TRACK =` / `def pct_rank` 定义，也无 `startswith(("5"/"6"/"9"))` 式前缀拼接（`_archive/` 除外）
  - 新增 `_common`：`market_of/em_secid/em_secucode/em_market`（东财 secid/secucode/market 唯一实现）+ 常量 `UA/WINDOW/IDX_DIV/ETF_TRACK`；`EM_UA = UA`
  - 改动点：前缀 6 处（`_fetch_stock_data`×2、`_fetch_etf_data`、`_fetch_etf_holdings`、`_update_summary`、`_fetch_watchlist`）；`UA` 4 处；`WINDOW` 3 处；`IDX_DIV` 3 处；`ETF_TRACK` 1 处（`_gen_analysis`→`_common`）
  - 自测：`_common` 扩到 **54 项 PASS**（新增东财标识 3 项 + 常量 1 项；首轮实测暴露 `market_prefix` 返回的是**带码前缀**而非市场名，已加 `market_of()` 修正）
  - 冒烟：`fetch_dividend("600036")`（em_secucode）25 条、`tencent_kline("515180")`（market_prefix）10 条；`push2.eastmoney.com` 端点本环境拒连（`RemoteDisconnected`，与本次改动无关，构造出的 secid 与旧写法逐字符相同）
  - 回归：`analysis.json`/`analysis_dy.json`/`dy_series.json` **字节未变**（纯常量搬移）；`_test_analysis.py --no-snapshot` 60/60 PASS；`update.py status` 正常
  - 说明：ETF「5 份元数据」中本轮收敛了 **track 映射**；`ETFS` 主清单（`_fetch_history`）、`FCF_ETFS`、名称映射各服务不同用途，未强行合并（避免改变数据结构）
- [x] **T24 update.py 去重复实现 + 抽 rebuild_web()** — 消除 6 份"重算 web 包"复制
  - 验收 ✅ 2026-09-09：新增 `rebuild_web(with_rec=True)`（指标→manifest→分析→评分→刷新 STOCKS→manifest v2），**5 处复制块**（`update_pool`/`update_watchlist` 的 rerun_web 分支、`retry_failed`、`deep_update` 与菜单的 check-fin 分支）全部改为调用它；`update.py` 内已无重复序列
  - 顺带统一：`load_json`/`save_json`/`safe_export` 三个本地实现删除，改 `_common` 的 `atomic_load`/`atomic_dump`/`safe_export` 别名（行为一致：异常兜底、tmp+replace 原子写、占用不中断）
  - 行为改进：check-fin 分支原来只重算评分+manifest（指标仍用旧财报），现一并重算指标与分析；提示语同步更新
  - 验证：`python update.py retry` 端到端跑通 `rebuild_web()`（日志见「评分结果无变化，跳过写盘」）→ **`git status` 仅剩 update.py 本身**，零数据/产物变更；`python update.py status` 正常
  - 未动 `update_web()`：它的 step 化流程把 components/summary/ETF持仓 穿插在指标与分析之间，与 rebuild_web 的顺序不同，强行合并会改变执行顺序（保留并注明）
- [x] **T25 清理退役 159229 残留** — `FCF_ETFS`、注释与特判分支
  - 验收 ✅ 2026-09-09：`FCF_ETFS` 移除 `159229`（8 只）；`cache/ETF持仓_159229.json` 一并删除；4 处误导注释改为当前口径（`_fetch_etf_holdings`×2、`_gen_analysis`、`_gen_web_data`）
  - 安全性核实：`成分_980092_股息率.json` 的 `etfs` 列表本就不含 159229（持仓重合度 <8 已被过滤），故估算值与产物**零漂移**；`scripts/` 中仅剩 1 处 159229（新注释说明为何移除）
  - 回归：`_gen_analysis.py` 重跑后 `analysis.json`/`analysis_dy.json`/`dy_series.json` **字节未变**；`_test_analysis.py --no-snapshot` 60/60 PASS

## 阶段七 · 工程化护栏（P1）

- [x] **T26 CI 跑 `_common` 自测** — 离线 0.1 s，覆盖前缀顺序事故点
  - 验收 ✅ 2026-09-09：`.github/workflows/daily-update.yml` 在「安装依赖」后新增 `python scripts/_common.py`（失败即中止，不进入数据更新）；实测**耗时 0.20 s**，**54 项断言 PASS**
  - 覆盖事故点：前缀顺序（92 先于 9x）、列式/稀疏解码往返、`pct_rank` 严格小于、东财 secid/secucode、陈旧检测阈值、原子写往返
- [x] **T27 新增 3 条关系断言** — 推荐20 分组==list、TOP20 因子覆盖率 ≥7/10、manifest 与 cache 一一对应
  - 验收 ✅ 2026-09-09：`_test_analysis.py` 新增「T10 关系断言」三条 → **63/63 PASS**（本地全量 65 项含 2 条快照）
  - 断言口径（**已按 T8 实测校正**）：① 回测「推荐20」**分组成员 == 评分产物 list**（20==20；比对的成员而非 summary 的 n——n 是"有信号样本数"，当前 18）；② TOP20 因子覆盖率 **≥7/10**（实测全 20 只均为 10/10；旧 920599 仅 2/10 会被拦下）；③ `manifest.stocks` **==** `cache/股票_*.json`（362==362）
  - 演练（人为造回归 → 变红）：向 manifest 注入假标的 999999 → `❌ manifest.stocks==cache/股票_* 差异: ['999999']`、**exit 1**；`git checkout` 还原 → 63/63 PASS、exit 0
  - 文档同步：README「62 项」→「65 项（CI 跑 63 项结构断言）」；CI 注释同步
  - ⚠️ 口径校正（T8 实测）：回测「推荐20」分组 n 是**有信号样本的标的数**，当前 **18/20**（920599 与另 2 只无样本，剔北交所前为 17），因此断言不能写等号；应改为「推荐20 分组成员 ⊆ list」+「list 中有样本者必须全部出现」
- [x] **T28 引入 ruff（F/E9/B）** — 首次即可抓出未用 import
  - 验收 ✅ 2026-09-09：新增 `pyproject.toml`（`select = ["F","E9","B"]`，排除 `scripts/_archive`）；CI 在数据更新前加 `pip install ruff && ruff check .`
  - 本环境 `pip install ruff` 被沙箱阻断（无法落盘 site-packages），故**用 AST 扫描等价复现** F 类首扫结果：**20 处未用 import**，全部清理（含计划点名的 `_fetch_stock_data.py:20 random`、`_backtest_analysis.py:13 io`，以及 `update.py json`、`_fetch_etf_holdings io/json`、`_gen_analysis io/datetime/decode_rows`、`_test_analysis io/math/np/fh` 等）
  - 复扫：未用 import **0**；`_test_analysis.py --no-snapshot` **63/63 PASS**；`_common.py` 自测 PASS；`py_compile` 全通过
  - ✅ 补验（沙箱放开后，2026-09-09）：`pip install ruff`（0.16.6）→ `ruff check .` 首扫 **33 处**并全部修完 → 现 **All checks passed!**（23×B007 未用循环变量改名 / 4×F841 删无用局部变量 / 3×F541 去空 f-string / 2×B905 补 `strict=False` / 1×B023 闭包显式绑定循环变量）
- [x] **T29 前端纯函数 node --test** — 覆盖 `analysis.js` / `reco.js`
  - 验收 ✅ 2026-09-09：新增 `tests/frontend/analysis.test.mjs`（6 项）+ `reco.test.mjs`（8 项）→ **14/14 PASS**
  - 覆盖：`scoreOf`（A/B 表权重、缺失因子归一、空输入）、`bandOf` 边界 25/45/65/80、`bandCls` 兜底、`RECO_W` 权重和=100、`recoBandOf` 边界 75/60/45、`groupOf` 五分组、`buildRecoPool`（过滤无回测/无信号、`crossPct`、`maxDate` 取池内 `manifest.last` 最大）、`btScoreOf` 公式（33.2）、`recoBaseOf`（dyPart/trig/diverge）、`recoScoreOf`（51.27 + 限幅）
  - 运行方式：`node --test --test-isolation=none tests/frontend/analysis.test.mjs tests/frontend/reco.test.mjs`（**必须 `--test-isolation=none`**：本沙箱禁止子进程管道 stdio，默认多进程模式会 `spawn EPERM`）
  - CI：新增 `actions/setup-node@v4`（node 22）+ 单测步骤（数据更新前执行）；README 测试章节同步

## 阶段八 · 回测研究有效性（P0）

- [x] **T30 回测执行日开关（--exec-offset）** — 默认 1 不变，可跑 +2 口径
  - 验收 ✅ 2026-09-09：`run_backtest(..., exec_offset=1)` 执行日索引与收益起点同步偏移（`+exec_offset`）；CLI 新增 `--exec-offset {1,2}`（默认 1）；**默认口径产物逐字段不变**（仅「生成日期」随系统时钟 09-08→09-09，已还原）
  - 非默认口径不覆盖产物：`exec_offset != 1` 时只算不写，并提示改用对比脚本（避免研究口径污染 `docs/回测报告.md`/`web/data/backtest.json`）
  - 顺带修复：`+exec_offset` 会让执行日越过序列末尾（信号落在最后几天）→ `IndexError`；已在窗口过滤后加 `buy_ex/sell_ex <= n-1` 截断（offset=1 时行为不变）
  - 重构：抽出 `collect_results(order, data, group_of, exec_offset, verbose)`，供 T31 对比脚本复用（不重复跑聚合逻辑）
- [x] **T31 产出 `docs/回测执行日对比.md`** — 指数/ETF/推荐20 的 1/3/6/12M 超额与胜率对比
  - 验收 ✅ 2026-09-09：新增 `scripts/_backtest_exec_compare.py`（只读分析池、跑两遍 p90 回测、**不写默认产物**）→ `docs/回测执行日对比.md`
  - 核心数据（12M 平均超额，t+1 → t+2）：**指数 +24.21% → +24.22%**（Δ+0.01pp）｜**ETF +33.51% → +33.95%**（Δ+0.43pp）｜**推荐20 +16.89% → +16.68%**（Δ−0.21pp）｜全量 +7.44% → +7.45%
  - 1/3/6M 与胜率同样接近（|Δ| ≤ 0.34pp 超额、≤ 1.1pp 胜率）；12M 正超额占比变化 ≤ 2pp；**无任何分组符号反转**
  - 报告含「五、决策记录」：三条判据（|Δ12M|<0.5pp 且不反转 / 正超额占比 ≤2pp / 胜率 ≤1.1pp）逐条判定，数据驱动可复现
- [x] **T32 默认口径决策门** — 据报告决定是否改默认口径（改则独立 commit + 全量重跑）
  - 决策 ✅ 2026-09-09：**维持默认口径 t+1（次一交易日收盘执行），不改**
  - 依据（三条判据全部通过，见 `docs/回测执行日对比.md` 第五节）：① 关键分组 12M 超额 |Δ| < 0.5pp 且无符号反转（指数 +24.21→+24.22、ETF +33.51→+33.95、推荐20 +16.89→+16.68）；② 12M 正超额占比变化 ≤ 2pp；③ 胜率 12M 变化 ≤ 1.1pp
  - 落地：`_backtest_analysis.build_report()` 增加「执行日口径审计」说明行（随报告生成，可复现）；AGENTS.md 回测口径补「执行日 t+1 + 审计结论」
  - 未改任何口径产物：`web/data/backtest.json` 仅 `date` 随系统时钟 09-08→09-09；数值逐字段与基线一致

---

## 下一期 TODO（2026-09-09 起）· Backlog 执行清单

> **来源**：本期 32 项执行过程中记录的本期外条目 + 现场发现的新缺口（含 T20/T24 的未覆盖面）
> **粒度**：功能点级（与本期一致）
> **状态标记**：`[ ]` 待办 · `[~]` 进行中 · `[x]` 完成 · `[!]` 阻塞
> **用法**：按 N 编号推进；每项完成即勾选 + 记录 commit；**未确认前不改代码**

### 现状快照（2026-09-09 基线，用于验收对账）

| 指标 | 当前值 | 备注 |
| :--- | :--- | :--- |
| `.git` 体积 | **663.6 MB**（pack 638 MB / 2 个 pack） | 另有 **28 个孤儿 idx + 6 个 tmp_pack**（见 N2） |
| `cache/` | 101.3 MB / 1497 文件 | 列式编码后（原 196.4 MB） |
| `web/data/` | 49.5 MB / 369 文件 | `stocks/` 45.7 MB / 362 文件 |
| `excel/` | 9.6 MB / 6 文件 | 内容未变则不重写（T20） |
| 池规模 | `manifest.stocks` 362 == `cache/股票_*` 362 == `web/data/stocks` 362 | `analysis.by_code` 385（11 指数 + 12 ETF + 362 股票）；`stale_n` 0 |
| 首屏载荷 | 扫描 ~132 KB / 推荐·持仓 ~283 KB / 对比页 dy ~423 KB（gzip） | 改造前 31.4 / 32.3 / 30.7 MB |
| 测试 | `_common` 54 · `_test_analysis` 65（CI 63）· 前端 14 · 浏览器冒烟 15 | 全部绿 |
| CI 前置 | `_common` 自测 + `ruff check .` + `node --test` | actionlint 1.7.12 通过 |

### 总览

| 阶段 | 主题 | 功能点 | 优先级 | 依赖 |
| :--- | :--- | :--- | :--- | :--- |
| 一 | 零噪声收尾与仓库卫生 | N1–N3 | P0 | — |
| 二 | 前端性能与体验 | N4–N8 | P1 | — |
| 三 | 数据链路效率 | N9–N13 | P1 | 阶段一 |
| 四 | 口径一致与文档 | N14–N17 | P2 | — |
| 五 | 工程化 | N18–N20 | P2 | 阶段一–四 |

---

## 阶段一 · 零噪声收尾与仓库卫生（P0）

- [x] **N1 回测/组合回测产物「无变化不写盘」** — 生成日期改数据日期，消除跨日 diff
  - 验收 ✅ 2026-09-09：四个产物的日期字段改为**数据日期**（`_backtest_analysis.pool_data_date()` = 分析池末行日期；`_backtest_portfolio` 用 `manifest.data_date`）；写盘统一走新增的 `_common.write_text_if_changed` / `atomic_dump_if_changed`
  - 实测：连续两次跑 `_backtest_analysis.py` + `_backtest_portfolio.py` → 第二次四个文件全部「内容未变，跳过写盘」，**SHA256 逐字节不变**；本次仅每文件 1 行内容变更（日期语义），无数值漂移
  - 前端：`backtestView`/`portfolioView` 标签「生成日期」→「数据日期」（浏览器实测两页均显示数据日期、无生成日期）
  - 回归：`_test_analysis.py` 63/63 PASS；`ruff check .` All checks passed；`tests/browser/smoke.mjs` 15/15 PASS
  - 现状/根因：`_backtest_analysis.build_report()` 与 `_backtest_portfolio` 每次运行都写 `docs/回测报告.md`/`web/data/backtest.json`/`docs/组合回测报告.md`/`web/data/portfolio_backtest.json`，其中「生成日期＝运行日」；无数据变化时跨日仍产生 diff → CI 会提交（**T20 只覆盖了 daily 链路**）
  - 做法：① 生成日期改为**数据日期**（`analysis.date` / 末行 K 线日期），另加「运行日期」字段但不参与比对；② 写盘复用「内容比对」思路（文本用 `write_text_if_changed`，JSON 用 `atomic_dump_if_changed(ignore=("date",…))`）
  - 验收：连续两日（无新数据）跑 `update.py bt` + `_backtest_portfolio.py` → `git status` 干净；有真实数据变化时正常提交且 diff 可读
- [ ] **N2 `.git` 卫生与体积治理** — 清孤儿对象 + repack + 体积监控（历史瘦身单独决策）
  - 现状：`.git` 663.6 MB（pack 638 MB / 2 个 pack）；**28 个孤儿 idx（无同名 pack）+ 6 个 tmp_pack**；`git fsck --connectivity-only` 无告警
  - 做法：① `git gc --prune=now`（或手工删孤儿 idx/tmp_pack）后复核 `git count-objects -vH`；② 新增体积监控（本地脚本或 CI 打印 count-objects）；③ **历史瘦身（filter-repo/BFG 删除历史大缓存快照）需单独决策**——会重写 SHA、要求所有人重新 clone
  - 验收：无 garbage 告警；记录清理前后 `.git` 体积；监控可复现
- [ ] **N3 快照漂移治理** — 让 CI 不必依赖 `--no-snapshot`
  - 现状：`_test_analysis.py` 的 2 条数值快照（T4.5/T7）随行情漂移，CI 用 `--no-snapshot` 跳过；本地全量测试常因漂移变红
  - 做法：① 逐条检查快照能否改为**区间/关系断言**；② 或提供 `--update-snapshot` 自动刷新并留痕；③ 明确 CI 与本地口径一致
  - 验收：CI 不带 `--no-snapshot` 连续两次运行稳定；本地全量 65 项可复现

## 阶段二 · 前端性能与体验（P1）

- [ ] **N4 前端数据内存 LRU** — `data.js` 的 `loadJSON` 当前无淘汰
  - 现状/根因：K线+指标单只约 0.5 MB，浏览 100 只 ≈ +470 MB 堆
  - 做法：LRU（条数或字节预算）；K线/指标优先淘汰，`manifest`/`analysis`/`dy_series` 进常驻白名单；淘汰后重进自动重载
  - 验收：连续浏览 200 只后堆稳定；切回已淘汰标的无报错（自动重载）
- [ ] **N5 dataZoom 拖动性能** — 长历史拖动掉帧
  - 现状/根因：`charts.js` 每帧 2×`getOption()` + 2×`replaceMerge` 整体重建（5000+ 点）
  - 做法：拖动中节流/抽帧（`requestAnimationFrame`）、只更新必要系列、避免全量 replaceMerge
  - 验收：拖动 5000 点序列 ≥30fps（Performance 抽样）；缩放/选中行为无回归
- [ ] **N6 compareView 串行请求改并发** — 8 只最多 24 次 RTT
  - 做法：`Promise.all` 分批（并发 4–6）+ 单只失败隔离（局部错误提示，不影响其余）
  - 验收：8 只对比首屏耗时下降 ≥50%；失败场景仍可读
- [ ] **N7 summaryView 搜索防抖** — 大表逐键过滤
  - 现状：`input` 直接 `applyFilter`（300+ 行表 + 环图重绘）
  - 做法：150–250 ms 防抖；必要时分页/虚拟滚动评估
  - 验收：连续输入不掉帧；结果与即时过滤一致
- [ ] **N8 主题切换免 reload** — 当前切主题会重下数据
  - 做法：CSS 变量热切换 + ECharts 主题重建（保留选中标的与缩放状态）
  - 验收：切换主题时 Network 无数据请求；视图状态保留

## 阶段三 · 数据链路效率（P1）

- [ ] **N9 抓取并行化** — 去掉串行等待
  - 现状：310 只串行 + 每只 0.8 s sleep（≈248 s 纯等待），东财接口 1 s/请求
  - 做法：腾讯源线程池（4–6 并发）+ 自适应节流；东财保持串行或小并发；保留失败熔断与失败清单
  - 验收：`daily` 总耗时下降 ≥40%；无新增失败项；缓存产物逐字段一致
- [ ] **N10 analysis_dy 磁盘往返消除** — 中间产物现在只服务后端
  - 现状：`_gen_analysis.main()` 写 `cache/analysis_dy.json`（1.91 MB）后 `build_factors()` 再读回；前端已改读 `analysis.json`/`dy_series.json`
  - 做法：内存直接传递；文件可保留（调试）或并入 `dy_series.json` 并瘦身
  - 验收：`analysis.json`/`dy_series.json` 逐字段一致；`analysis_dy.json` 可删除或减半
- [ ] **N11 Excel 一次写出** — 当前两遍 IO
  - 现状：pandas 写 → openpyxl 重新加载 → 美化 → 保存
  - 做法：xlsxwriter 或 openpyxl write_only 一次成型（列宽/冻结/筛选/日期格式）
  - 验收：单元格逐一致；导出耗时下降
- [ ] **N12 `_fetch_etf_data.py` 导入副作用清理** — 模块顶层直接跑扫描并打印
  - 现状：`update.py` 仅为取 `tencent_quote` 而 import 它，会连带打印整张 ETF 表
  - 做法：可执行部分收进 `main()` / `if __name__ == "__main__"`，只暴露函数
  - 验收：`import _fetch_etf_data` 无输出、无网络；`update.py daily` 日志不再出现该表
- [ ] **N13 `update_web()` 与 `rebuild_web()` 收敛** — T24 遗留
  - 现状：`update_web()` 的 step 流程把 components/summary/ETF持仓 穿插在指标与分析之间，仍是独立实现
  - 做法：把步骤序列参数化（`rebuild_web(steps=[…])`），或让 `update_web` 复用同一序列并显式声明差异
  - 验收：两条路径产物一致；`update.py web` / `retry` 行为不变

## 阶段四 · 口径一致与文档（P2）

- [ ] **N14 页面公示公式与实现对齐** — `recommendView.js:76` vs `reco.js:86`
  - 现状：页面文案的权重/分项说明与 `btScoreOf` 实现漂移
  - 做法：以代码为准更新文案，或抽公共常量（如 `RECO_BT_W`）供两侧引用
  - 验收：文案与代码单一来源；人工核对一屏
- [ ] **N15 文档漂移修正** — README / AGENTS.md / 困难总结
  - 现状：README（池规模、断言数 65/63、视图数 10）、AGENTS.md（视图数、脚本表、`_fetch_etf_data` 描述）与实现存在漂移
  - 做法：逐条核对 `update.py status` / `manifest.json` / 测试输出；困难总结继续追加编号（当前 147+）
  - 验收：文档数字与产物一致；新增条目有编号
- [ ] **N16 孤儿数据分支迁移评估（D4 遗留）** — cache/web/data 与代码同分支
  - 做法：评估 orphan `data` 分支 / LFS / 外部对象存储的收益与代价（克隆体验、CI 推送、本地开发）
  - 验收：一份方案文档 + 不改主分支历史的试点结论
- [ ] **N17 docs 生成物与研究文档分离** — 边界不清
  - 现状：`docs/` 混放自动生成物（`回测报告.md`/`组合回测报告.md`/`回测执行日对比.md`）与手写研究笔记
  - 做法：生成物加「自动生成」头注 + 目录分区（如 `docs/gen/`）或 `.gitattributes` 标注
  - 验收：人工/生成边界清晰；重生成不覆盖手写内容

## 阶段五 · 工程化（P2）

- [ ] **N18 浏览器冒烟进 CI** — `tests/browser/smoke.mjs` 目前仅本地
  - 做法：独立 job 用 `windows-latest`（自带 Edge）+ `npm i playwright-core`；或 matrix 只在 windows 跑
  - 验收：CI 上 15 项浏览器断言绿；失败可见
- [ ] **N19 ruff 规则扩展** — 当前仅 F/E9/B
  - 做法：分批开 I（import 排序）/ UP / SIM / C4；每批 `--fix` + 人工复核；CI 固定 ruff 版本避免漂移
  - 验收：每批修复后全绿；记录规则集版本
- [ ] **N20 后端关键函数单测** — 目前只有端到端产物断言
  - 现状：`_backtest_analysis.merge_close`、`_backtest_portfolio.pick_at`、`_recommend_stocks.apply_constraints` 等关键函数无单测
  - 做法：新增 `tests/python/`（pytest/unittest）+ 小型 fixture，进 CI
  - 验收：覆盖关键分支；改动这些函数时能被拦住

### 暂缓 / 需单独决策（不在 N1–N20 范围）

- **历史瘦身（重写 SHA）**：可显著缩小 `.git`，但要求所有人重新 clone、破坏旧提交引用 → 需单独评估与授权（N2 ③）
- **前端引入构建工具/框架**：纯静态 ESM（无构建）是刻意设计，除非有明确收益否则不动
- **引入服务端 API/数据库**：当前架构「Python 预计算 + 静态前端」是刻意选择，改动面大

---

## 完成标准（本期 T1–T32 自检，已完成）

| 阶段 | 自检命令 | 结果 |
| :--- | :--- | :--- |
| 一 | `python update.py status` + 演练截断标的 | ✅ |
| 二 | `python update.py daily --yes` + `python scripts/_test_analysis.py` | ✅（三次 daily 792/777/785 s） |
| 三 | `curl -sI -H "Accept-Encoding: gzip" …` + Edge headless Network 断言 | ✅（HTTP + `tests/browser/smoke.mjs` 15 项） |
| 四 | 逐字段对账脚本 + `python scripts/_test_analysis.py` | ✅（1528 cache + 374 指标逐字段 0 差异） |
| 五 | `git status --short` 干净 + `git count-objects -vH` | ✅（空跑零提交；`.git` 待治理见 N2） |
| 六 | `python scripts/_common.py` + 快照刷新 | ✅（54 项） |
| 七 | 人为造回归 → CI 变红 | ✅（注入假标的 → exit 1） |
| 八 | 对比报告产出 + 决策记录 | ✅（维持 t+1） |

## 完成标准（下一期 N1–N20 自检）

| 阶段 | 自检命令 / 判据 |
| :--- | :--- |
| 一 零噪声/仓库卫生 | 连续两日（无新数据）跑 `update.py bt` + `_backtest_portfolio.py` 后 `git status` 干净；`git count-objects -vH` 无 garbage |
| 二 前端性能 | DevTools Performance 抽样（帧率/堆）+ `node tests/browser/smoke.mjs` 全绿 |
| 三 数据链路 | `python update.py daily --yes` 耗时对比（≥40% 降幅）+ 产物逐字段对账 |
| 四 口径/文档 | 页面文案与 `reco.js` 常量对照 + 文档数字与 `update.py status` 一致 |
| 五 工程化 | CI 变红演练 + `ruff check .` + `node --test` + `pytest`（新增） |

---

*本文档为执行顺序清单：上半部分为本期 T1–T32 完成记录，下半部分为下一期 N1–N20 待办。*
*文档本身未修改任何代码或数据。*
