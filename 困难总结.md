# 红利指数/ETF研究项目 · 过程困难总结

> 项目时间：2026-08-02 · 内容：成分股获取（10指数+11ETF）→ 指数更正（980092→932368）→ 历史行情拉取（缓存+Excel）→ 选股推荐（20只）→ 成分股汇总表（Excel+update.py集成）
> 对应文件：`红利介绍.md` / `红利指数与ETF成分股.md` / `红利股票推荐20只.md` / `excel/红利成分股汇总.xlsx` / `update.py` / `scripts/_update_summary.py` / `cache/` / `excel/`

---

## 一、成分股获取阶段（最艰难）

### 1. 东财 push2 被 IP 级风控
- **现象**：`RemoteDisconnected` 连接被重置，push2 / push2his / 1~99.push2 数字子域全家族无一幸免
- **影响**：最常用的"指数成分股"接口全部不可用
- **突破口**：发现 `datacenter-web.eastmoney.com` 是**不同子域、不同WAF**，未被封——东财按子域隔离风控

### 2. datacenter 成分表覆盖不全
- `RPT_INDEX_COMPONENT` 只覆盖11个小众指数，缺 000821 / 000825 / H30269 / H30270 / 980092 五个
- 且只能拿到代码（SECURITY_CODE），无名称、无权重

### 3. 中证官网改版逆向难
- 新版 SPA 架构，所有已知老接口（老版 cons.xls 路径、akshare 接口）全部 404
- 逆向 webpack：app.js → chunk 哈希映射 → 下载 chunk 逐个搜索接口路径，耗费多轮
- `index-sample-information` 暴力测试 10+ 种参数组合全部返回空（该接口其实是"证券所属指数检索"页用的）

### 4. 第三方源全军覆没
| 候选源 | 结果 |
| :-- | :-- |
| 雪球 index_members | 需 cookie |
| 蛋卷 danjuan | 需登录 |
| 同花顺 q.10jqka | 404 |
| iwencai 网页接口 | 403 Nginx forbidden |
| 乐咕乐股 legulegu | 404 |
| mootdx（通达信 TCP） | 全部服务器超时/取数失败 |
| 腾讯指数成分接口 | controller 不存在 / 404 |

### 5. 最终突破口：CloakBrowser 监听网络请求
- 渲染中证官网指数详情页 → **监听网络请求** → 发现 `index-details-data` 接口返回官方样本文件下载链接（oss-ch.csindex.com.cn）
- 9 个中证指数全量成分+权重 Excel 一次搞定（`{code}cons.xls` 成分 + `{code}closeweight.xls` 权重）
- 980092（国证）：国证官网对 requests 超时/404，但 CloakBrowser 页面上下文内 fetch 可用

### 6. ETF 持仓接口连环失效
- fundmobapi 限流"网络繁忙"（61136403）
- fundf10 老接口 FundArchivesDatas.aspx 404（api.fund.eastmoney.com 同样 404）
- 最终方案：CloakBrowser 抓天天基金持仓页（ccmx_{code}.html）

---

## 二、指数更正阶段（980092 → 932368）

### 7. 指数"名实不符"
- 159229 实际跟踪的是**中证800自由现金流（932368）**，而非文档原写的国证自由现金流（980092）
- **发现方法**：交叉比对——159229 季报重仓（格力9.87%领衔）与 980092 指数权重（中国电信9.05%居首）明显矛盾；经天天基金基金档案"跟踪标的"字段证实
- 教训：ETF 持仓 vs 指数权重交叉验证，能揪出名实不符的坑

### 8. 差点毁掉文档的脚本事故
- 生成脚本解析 md 时未处理标题后的空行，9 个指数解析出 0 只
- 第二次脚本解析失败后，在 ETF 循环抛 `KeyError: '000151'`——**恰好阻止了坏文件写入**（侥幸）
- 修复：解析时跳过空行后再定位引用行与表格

---

## 三、历史行情拉取阶段

### 9. 腾讯 K 线不认 5 个中证代码
- H30269 / 930955 / 932315 / 931468 / H30270 在腾讯全部空数据/结构异常
- 只能走中证官网 `perf/index-perf` 接口

### 10. 中证官网限流严重
- 连续请求返回空响应（JSONDecodeError），间隔 0.6s 也触发风控
- CloakBrowser 页面内 fetch 也被 WAF 拦截（403 attack 页面），`page.request.get` 同样 403
- **最终方案**：纯 requests + 低速重试（失败后 8s/18s/28s... 递增退避，最多8次），按年分段拉取，全程约 30 分钟

### 11. ETF 净值接口连环踩坑
- fundmobapi `FundMNHisNetList`：限流返回空
- 天天基金 lsjz：pageSize 参数被忽略，固定 20 条/页（512890 需 92 页，11 只共 1000+ 请求不可行）
- **最终方案**：新浪 `CaihuiFundInfoService.getNav`，`num=1000` 生效，2 页/只搞定

### 12. 国证 980092 历史K线缺失
- 腾讯只返回 1 根（2026-07-31）
- 国证官网详情页监听请求发现 `hq.cnindex.com.cn/market/market/getIndexDailyDataWithDataFormat`，可用

### 13. 两个缓存工程事故
- **事故A**：`--refresh` 全量重拉时撞上限流，**0 条结果直接覆盖了 H30269 的 5016 条好缓存**（随后靠低速重试恢复）
- **事故B**：低速补拉脚本合并 bug——新拉数据用 `tradeDate` 字段、旧缓存用 `date` 字段，规范化不一致导致 930955 旧数据（3904 条）全被挤进空 key 丢失，修复字段统一后重拉恢复（最终 4773 条）

### 14. bash 超时限制
- 单条命令 900s 超时上限，中证官网低速拉取被迫分批跑（每次 2 个指数）

---

## 四、增量更新与数据维护阶段（后续迭代）

### 15. 新浪净值 datefrom 参数不生效 → ETF缓存K线被抹掉（重大事故）
- **现象**：ETF Excel 中开盘/收盘/最高/最低/成交量/成交额全部变空
- **根因链**：新浪 `getNav` 的 `datefrom` 参数实测**被忽略**（返回全量历史倒序）→ 增量拉取时净值拿到全部、K线只拿到最后1天 → 增量合并时“新行整体覆盖旧行”→ 缓存中 1800+ 条 K 线被只有净值字段的行覆盖
- **修复**：① 新浪净值在本地过滤 `date >= start`；② `update_incremental` 改为**字段级合并**——新值为空(None)的字段保留旧值，防止不完整增量行破坏缓存；③ 删除被毁缓存全量重拉恢复

### 16. 中证官网限流恶化：成交额补充尝试失败
- 为补齐腾讯源 5 只指数的成交额，尝试将其切换为中证官网源（官方 tradingValue 有值，与红利介绍.md 599.27亿交叉验证一致）
- **限流持续数十分钟**：单年请求反复失败、等待冷却 10 分钟仍无效；CloakBrowser 页面内 fetch 被 WAF 拦（attack.jinxibei.com 风控页）；网易 502、百度 403
- **结论**：放弃切换，保留腾讯源，指数成交额缺失即缺失（指数点位非价格，无法估算）

### 17. 字段名不一致的坑第二次重犯
- `_pull_missing.py` 的 norm() 只读 `tradeDate`，而旧缓存字段是 `date` → 合并结果为空 → IndexError
- 与事故B（第13条）同源——**旧缓存与新拉数据的字段规范必须统一**，本次又是写成新脚本时忘了兼容

### 18. 参数解析 bug 产生垃圾文件
- `_pull_missing.py` 的 sys.argv 解析把日期参数误当代码，生成 `cache/指数_20190101.json` 垃圾缓存

### 19. stdout 重复包装导致崩溃
- `update.py` import `_fetch_history` 时，模块级 `sys.stdout = TextIOWrapper(...)` 二次包装共用底层 buffer，旧包装对象被 GC 时关闭 buffer → `I/O operation on closed file`
- **修复**：stdout 包装只在 `if __name__ == "__main__"` 下执行

### 20. sed 全局替换误伤
- 用 sed 替换 `if "nav" in df.columns` 时把所有匹配处（含指数块）一起替换，列顺序重排逻辑加错位置；且 rename 后列名已是中文，判断需用中文列名

### 21. Excel 文件被占用
- Excel 导出时若文件正被 Excel/WPS 打开 → `PermissionError` 崩溃 → 改为捕获并提示“请关闭后重试”

---

## 五、选股推荐与成分股汇总阶段（后续迭代）

### 22. push2 主站再次被封，push2delay 可用（同源不同域名）
- **现象**：选股任务补行业/估值时，push2 `clist`（全市场清单）与 `stock/get`（逐只）全部 `RemoteDisconnected`，与第一阶段同款 IP 级风控
- **降级**：发现 `push2delay.eastmoney.com`（延迟行情，同接口协议）可用——f127 行业、f43 价格、f162 动态PE、f167 PB、f116 总市值均正常
- **字段坑**：push2delay **不返回 f9/f23（PE/PB）和 f133（股息率）**，只返回 f43/f57/f58/f116/f117/f127/f162/f167 → 估值必须走腾讯，股息率必须走 datacenter

### 23. 腾讯行情 prefix 路由 bug：北交所 92 号段被 9x 规则抢走（坑第二次）
- **现象**：920599/920509 两只北交所股永远查不到数据（腾讯返回空）
- **根因**：prefix 函数 `if c.startswith(("5","6","9")): return "sh"+c` 写在 92 判断之前 → 920599 被路由成 `sh920599` → 空
- **教训**：skill 已明确警告“92 号段必须先于 9x 判断”，自己写路由时仍重犯——**凡是踩过的坑，写成代码时一定要按文档顺序逐条核对**
- 修复：腾讯全量重拉（几秒）后，920599 PE=10.65/PB=2.3，与 push2delay 交叉验证一致

### 24. 分红接口金额单位是“每10股”不是“每股”
- **现象**：股息率首轮算出一堆 40-50% 的荒谬值（伊利 50.9%、招行 50.9%）
- **根因**：`RPT_SHAREBONUS_DET` 的 `PRETAX_BONUS_RMB` 是**每10股税前派息**（招行 20 = 每股2.0元），直接当每股算大了 10 倍
- **修复**：`bonus / 10` 后再累加；修正后招行 5.09%、伊利 5.09%，合理

### 25. “近12个月无派息”≠“数据缺失”
- 兰花科创/中集集团/天合光能最近一次除权日在 12 个月窗口外（中集/天合甚至亏损两年未分红）→ 股息率按“实际派息=0”记 **0.00%** 并保留历史分红记录，而不是留空
- 教训：缺失 vs 为 0 必须在数据字典里显式区分，否则 Excel 空值会误导读者

### 26. 行业字段是申万二级/三级，不是东财三级
- 东财 f127 实测返回申万分类（银行Ⅱ、白酒Ⅱ、中药Ⅱ），第一次映射表按东财三级名（银行Ⅲ）写 → 0 命中
- 修复：改为关键词包含匹配（按命中键长度降序消除“石油 vs 油气开采”歧义），28 个一级行业归并完成

### 27. 权重未公开的指数导致“主要入选指数”列全空
- 20 只股票只出现在中证800自由现金流（932368）和上证国有企业红利（000151）——**两个指数的权重均为“—”未公开**
- 修复：标注“指数名(权重未公开)”而非留空；同时负 PE（中集集团 -410）是真实亏损，保留并说明

### 28. 长批量任务先探测连通性 + 连续失败快速中止（重要经验）
- 全量 289 只分红拉取预计 6 分钟，跑之前**先用 1 个请求探测 datacenter 是否被封**（0.2s 响应确认可用才继续）
- 循环内加“连续 8 次失败自动中止 + 每 50 只自动存盘”，避免 IP 被封后白白空等数分钟
- 经验：长时间等待前先探测；被封后重试无益，应立即降级或中止

### 29. 缓存字段不同步
- backfill 把估值写进 `_成分股汇总.json`（t_pe/t_pb/t_mcap），但 `_成分股汇总表.json` 的 pe/pb/mcap 仍是旧值 → 重新构建汇总表时必须以“全量缓存”为唯一事实源，而非复制旧表

### 30. openpyxl 样式赋值踩坑 + 编辑事故
- `cell.fill = A if cond else None` 传 None 直接崩溃（`TypeError: Fill() takes no arguments`）→ 改条件分支赋值
- 编辑脚本时误删换行导致 run() 函数体缩进错乱（IndentationError）——**改完代码立即 `py_compile` 验证**再继续

### 31. 可复用模块必须函数化 + 增量更新设计
- `_gen_summary_excel.py` 顶层直接执行，无法被 update.py import → 重构时把逻辑全部收进 `_update_summary.run()`（带 `__name__` 保护）
- 增量策略：行业/分红只拉新增股票（每只约1.2s）、腾讯行情全量刷新（批量秒级）、`--force` 才全量重算分红 → 日常更新 0.1 分钟完成

---

## 关键经验清单

1. **东财按子域隔离风控**：push2 封了不代表 datacenter-web 也封，可降级切换
2. **CloakBrowser 监听网络请求**是逆向 SPA 数据接口的最快路径，远快于逆向 JS
3. **数据正确性靠交叉验证**：ETF 持仓 vs 指数权重比对，揪出 159229 跟踪指数错误
4. **写缓存前先想覆盖语义**：拉取失败返回 0 条时绝不能覆盖已有好缓存；合并新旧数据必须先统一字段规范（此坑踩过两次）
5. **增量更新必须字段级合并**：新行整体覆盖旧行 = 一次不完整拉取就能毁掉全部缓存；新值为空时保留旧值
6. **官网文件权限策略差异**：中证官网部分指数（000151/932365/932368）样本文件不公开（403），需东财成分表+官网十大权重拼装
7. **接口分页/频率参数陷阱**：lsjz 的 pageSize 被忽略、新浪 datefrom 不生效、index-perf 有隐藏限流——先小样本实测再全量
8. **可被 import 的脚本不要做模块级副作用**：stdout 包装等只放 `__main__`，否则被其他模块 import 时二次包装崩溃
9. **工具脚本谨慎用 sed 全局替换**：多文件多位置替换前先确认目标位置，替换后立即检查
10. **push2delay 是 push2 被封时的同协议降级**：但字段不全（无 f9/f23/f133），估值走腾讯、股息率走 datacenter
11. **北交所 92 号段 prefix 路由必须优先于 9x**：此坑踩过两次，写路由函数时按文档顺序逐条核对
12. **分红接口金额单位是“每10股”**：`PRETAX_BONUS_RMB` 必须 ÷10 才是每股；股息率算出来 40-50% 就是单位错了
13. **“窗口内无派息”记 0 而非空**：数据缺失与数值为 0 要显式区分
14. **长批量任务三步法**：先单请求探测连通性 → 连续失败快速中止（8次）→ 每 N 条自动存盘；不要干等，等再久也大概率是 IP 被封
15. **缓存同步以“唯一事实源”为准**：多份缓存间派生字段（pe/pb/mcap）重建时从全量缓存重算，不复制旧表
16. **可被 import 的脚本必须函数化 + `__main__` 保护**：顶层副作用代码无法被 update.py 复用
17. **改完代码立即编译验证**：`python -c "import py_compile; py_compile.compile('x.py', doraise=True)"` 防缩进/语法事故
