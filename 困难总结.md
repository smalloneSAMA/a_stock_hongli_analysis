# 红利指数/ETF研究项目 · 过程困难总结

> 项目时间：2026-08-02 ~ 08-03 · 内容：成分股获取（10指数+11ETF）→ 指数更正（980092→932368）→ 历史行情拉取（缓存+Excel）→ 选股推荐（20只）→ 成分股汇总表（Excel+update.py集成）→ 推荐20只股票历史数据（2004年起，估值指标逐日计算）
> 对应文件：`红利介绍.md` / `红利指数与ETF成分股.md` / `红利股票推荐20只.md` / `excel/红利成分股汇总.xlsx` / `update.py` / `scripts/_update_summary.py` / `scripts/_fetch_stock_data.py` / `cache/` / `excel/`

---

## 一、成分股获取阶段（最艰难）

### 1. 东财 push2 被 IP 级风控
- **现象**：`RemoteDisconnected` 连接被重置，push2 / push2his / 1~99.push2 数字子域全家族无一幸免
- **影响**：最常用的"指数成分股"接口全部不可用
- **突破口**：发现 `datacenter-web.eastmoney.com` 是**不同子域、不同WAF**，未被封——东财按子域隔离风控

### 2. datacenter 成分表覆盖不全
- `RPT_INDEX_COMPONENT` 只覆盖11个小众指数，缺 000821 / 000825 / H30269 / H30270 / 980092 五个
- 且只能拿到代码（SECURITY_CODE），无名称、无权重

### 3. 中证官网改版逆向难
- 新版 SPA 架构，所有已知老接口（老版 cons.xls 路径、akshare 接口）全部 404
- 逆向 webpack：app.js → chunk 哈希映射 → 下载 chunk 逐个搜索接口路径，耗费多轮
- `index-sample-information` 暴力测试 10+ 种参数组合全部返回空（该接口其实是"证券所属指数检索"页用的）

### 4. 第三方源全军覆没
| 候选源 | 结果 |
| :-- | :-- |
| 雪球 index_members | 需 cookie |
| 蛋卷 danjuan | 需登录 |
| 同花顺 q.10jqka | 404 |
| iwencai 网页接口 | 403 Nginx forbidden |
| 乐咕乐股 legulegu | 404 |
| mootdx（通达信 TCP） | 全部服务器超时/取数失败 |
| 腾讯指数成分接口 | controller 不存在 / 404 |

### 5. 最终突破口：CloakBrowser 监听网络请求
- 渲染中证官网指数详情页 → **监听网络请求** → 发现 `index-details-data` 接口返回官方样本文件下载链接（oss-ch.csindex.com.cn）
- 9 个中证指数全量成分+权重 Excel 一次搞定（`{code}cons.xls` 成分 + `{code}closeweight.xls` 权重）
- 980092（国证）：国证官网对 requests 超时/404，但 CloakBrowser 页面上下文内 fetch 可用

### 6. ETF 持仓接口连环失效
- fundmobapi 限流"网络繁忙"（61136403）
- fundf10 老接口 FundArchivesDatas.aspx 404（api.fund.eastmoney.com 同样 404）
- 最终方案：CloakBrowser 抓天天基金持仓页（ccmx_{code}.html）

---

## 二、指数更正阶段（980092 → 932368）

### 7. 指数"名实不符"
- 159229 实际跟踪的是**中证800自由现金流（932368）**，而非文档原写的国证自由现金流（980092）
- **发现方法**：交叉比对——159229 季报重仓（格力9.87%领衔）与 980092 指数权重（中国电信9.05%居首）明显矛盾；经天天基金基金档案"跟踪标的"字段证实
- 教训：ETF 持仓 vs 指数权重交叉验证，能揪出名实不符的坑

### 8. 差点毁掉文档的脚本事故
- 生成脚本解析 md 时未处理标题后的空行，9 个指数解析出 0 只
- 第二次脚本解析失败后，在 ETF 循环抛 `KeyError: '000151'`——**恰好阻止了坏文件写入**（侥幸）
- 修复：解析时跳过空行后再定位引用行与表格

---

## 三、历史行情拉取阶段

### 9. 腾讯 K 线不认 5 个中证代码
- H30269 / 930955 / 932315 / 931468 / H30270 在腾讯全部空数据/结构异常
- 只能走中证官网 `perf/index-perf` 接口

### 10. 中证官网限流严重
- 连续请求返回空响应（JSONDecodeError），间隔 0.6s 也触发风控
- CloakBrowser 页面内 fetch 也被 WAF 拦截（403 attack 页面），`page.request.get` 同样 403
- **最终方案**：纯 requests + 低速重试（失败后 8s/18s/28s... 递增退避，最多8次），按年分段拉取，全程约 30 分钟

### 11. ETF 净值接口连环踩坑
- fundmobapi `FundMNHisNetList`：限流返回空
- 天天基金 lsjz：pageSize 参数被忽略，固定 20 条/页（512890 需 92 页，11 只共 1000+ 请求不可行）
- **最终方案**：新浪 `CaihuiFundInfoService.getNav`，`num=1000` 生效，2 页/只搞定

### 12. 国证 980092 历史K线缺失
- 腾讯只返回 1 根（2026-07-31）
- 国证官网详情页监听请求发现 `hq.cnindex.com.cn/market/market/getIndexDailyDataWithDataFormat`，可用

### 13. 两个缓存工程事故
- **事故A**：`--refresh` 全量重拉时撞上限流，**0 条结果直接覆盖了 H30269 的 5016 条好缓存**（随后靠低速重试恢复）
- **事故B**：低速补拉脚本合并 bug——新拉数据用 `tradeDate` 字段、旧缓存用 `date` 字段，规范化不一致导致 930955 旧数据（3904 条）全被挤进空 key 丢失，修复字段统一后重拉恢复（最终 4773 条）

### 14. bash 超时限制
- 单条命令 900s 超时上限，中证官网低速拉取被迫分批跑（每次 2 个指数）

---

## 四、增量更新与数据维护阶段（后续迭代）

### 15. 新浪净值 datefrom 参数不生效 → ETF缓存K线被抹掉（重大事故）
- **现象**：ETF Excel 中开盘/收盘/最高/最低/成交量/成交额全部变空
- **根因链**：新浪 `getNav` 的 `datefrom` 参数实测**被忽略**（返回全量历史倒序）→ 增量拉取时净值拿到全部、K线只拿到最后1天 → 增量合并时“新行整体覆盖旧行”→ 缓存中 1800+ 条 K 线被只有净值字段的行覆盖
- **修复**：① 新浪净值在本地过滤 `date >= start`；② `update_incremental` 改为**字段级合并**——新值为空(None)的字段保留旧值，防止不完整增量行破坏缓存；③ 删除被毁缓存全量重拉恢复

### 16. 中证官网限流恶化：成交额补充尝试失败
- 为补齐腾讯源 5 只指数的成交额，尝试将其切换为中证官网源（官方 tradingValue 有值，与红利介绍.md 599.27亿交叉验证一致）
- **限流持续数十分钟**：单年请求反复失败、等待冷却 10 分钟仍无效；CloakBrowser 页面内 fetch 被 WAF 拦（attack.jinxibei.com 风控页）；网易 502、百度 403
- **结论**：放弃切换，保留腾讯源，指数成交额缺失即缺失（指数点位非价格，无法估算）

### 17. 字段名不一致的坑第二次重犯
- `_pull_missing.py` 的 norm() 只读 `tradeDate`，而旧缓存字段是 `date` → 合并结果为空 → IndexError
- 与事故B（第13条）同源——**旧缓存与新拉数据的字段规范必须统一**，本次又是写成新脚本时忘了兼容

### 18. 参数解析 bug 产生垃圾文件
- `_pull_missing.py` 的 sys.argv 解析把日期参数误当代码，生成 `cache/指数_20190101.json` 垃圾缓存

### 19. stdout 重复包装导致崩溃
- `update.py` import `_fetch_history` 时，模块级 `sys.stdout = TextIOWrapper(...)` 二次包装共用底层 buffer，旧包装对象被 GC 时关闭 buffer → `I/O operation on closed file`
- **修复**：stdout 包装只在 `if __name__ == "__main__"` 下执行

### 20. sed 全局替换误伤
- 用 sed 替换 `if "nav" in df.columns` 时把所有匹配处（含指数块）一起替换，列顺序重排逻辑加错位置；且 rename 后列名已是中文，判断需用中文列名

### 21. Excel 文件被占用
- Excel 导出时若文件正被 Excel/WPS 打开 → `PermissionError` 崩溃 → 改为捕获并提示“请关闭后重试”

---

## 五、选股推荐与成分股汇总阶段（后续迭代）

### 22. push2 主站再次被封，push2delay 可用（同源不同域名）
- **现象**：选股任务补行业/估值时，push2 `clist`（全市场清单）与 `stock/get`（逐只）全部 `RemoteDisconnected`，与第一阶段同款 IP 级风控
- **降级**：发现 `push2delay.eastmoney.com`（延迟行情，同接口协议）可用——f127 行业、f43 价格、f162 动态PE、f167 PB、f116 总市值均正常
- **字段坑**：push2delay **不返回 f9/f23（PE/PB）和 f133（股息率）**，只返回 f43/f57/f58/f116/f117/f127/f162/f167 → 估值必须走腾讯，股息率必须走 datacenter

### 23. 腾讯行情 prefix 路由 bug：北交所 92 号段被 9x 规则抢走（坑第二次）
- **现象**：920599/920509 两只北交所股永远查不到数据（腾讯返回空）
- **根因**：prefix 函数 `if c.startswith(("5","6","9")): return "sh"+c` 写在 92 判断之前 → 920599 被路由成 `sh920599` → 空
- **教训**：skill 已明确警告“92 号段必须先于 9x 判断”，自己写路由时仍重犯——**凡是踩过的坑，写成代码时一定要按文档顺序逐条核对**
- 修复：腾讯全量重拉（几秒）后，920599 PE=10.65/PB=2.3，与 push2delay 交叉验证一致

### 24. 分红接口金额单位是“每10股”不是“每股”
- **现象**：股息率首轮算出一堆 40-50% 的荒谬值（伊利 50.9%、招行 50.9%）
- **根因**：`RPT_SHAREBONUS_DET` 的 `PRETAX_BONUS_RMB` 是**每10股税前派息**（招行 20 = 每股2.0元），直接当每股算大了 10 倍
- **修复**：`bonus / 10` 后再累加；修正后招行 5.09%、伊利 5.09%，合理

### 25. “近12个月无派息”≠“数据缺失”
- 兰花科创/中集集团/天合光能最近一次除权日在 12 个月窗口外（中集/天合甚至亏损两年未分红）→ 股息率按“实际派息=0”记 **0.00%** 并保留历史分红记录，而不是留空
- 教训：缺失 vs 为 0 必须在数据字典里显式区分，否则 Excel 空值会误导读者

### 26. 行业字段是申万二级/三级，不是东财三级
- 东财 f127 实测返回申万分类（银行Ⅱ、白酒Ⅱ、中药Ⅱ），第一次映射表按东财三级名（银行Ⅲ）写 → 0 命中
- 修复：改为关键词包含匹配（按命中键长度降序消除“石油 vs 油气开采”歧义），28 个一级行业归并完成

### 27. 权重未公开的指数导致“主要入选指数”列全空
- 20 只股票只出现在中证800自由现金流（932368）和上证国有企业红利（000151）——**两个指数的权重均为“—”未公开**
- 修复：标注“指数名(权重未公开)”而非留空；同时负 PE（中集集团 -410）是真实亏损，保留并说明

### 28. 长批量任务先探测连通性 + 连续失败快速中止（重要经验）
- 全量 289 只分红拉取预计 6 分钟，跑之前**先用 1 个请求探测 datacenter 是否被封**（0.2s 响应确认可用才继续）
- 循环内加“连续 8 次失败自动中止 + 每 50 只自动存盘”，避免 IP 被封后白白空等数分钟
- 经验：长时间等待前先探测；被封后重试无益，应立即降级或中止

### 29. 缓存字段不同步
- backfill 把估值写进 `_成分股汇总.json`（t_pe/t_pb/t_mcap），但 `_成分股汇总表.json` 的 pe/pb/mcap 仍是旧值 → 重新构建汇总表时必须以“全量缓存”为唯一事实源，而非复制旧表

### 30. openpyxl 样式赋值踩坑 + 编辑事故
- `cell.fill = A if cond else None` 传 None 直接崩溃（`TypeError: Fill() takes no arguments`）→ 改条件分支赋值
- 编辑脚本时误删换行导致 run() 函数体缩进错乱（IndentationError）——**改完代码立即 `py_compile` 验证**再继续

### 31. 可复用模块必须函数化 + 增量更新设计
- `_gen_summary_excel.py` 顶层直接执行，无法被 update.py import → 重构时把逻辑全部收进 `_update_summary.run()`（带 `__name__` 保护）
- 增量策略：行业/分红只拉新增股票（每只约1.2s）、腾讯行情全量刷新（批量秒级）、`--force` 才全量重算分红 → 日常更新 0.1 分钟完成

---

## 六、推荐20只股票历史数据与估值指标阶段（08-03）

### 32. 腾讯 K 线 start 参数无效（增量机制被推翻重设计）
- **实测**：`param=sh600036,day,2004-01-01,,800,qfq` 返回的是**最近 800 条**（2023 年起），start 被静默忽略
- **影响**：无法按日期增量拉取；但翻页用 end 参数向前翻**页间无缝**（页2最新=页1最早）
- **方案**：全量=end 翻页+过滤起点；增量=只拉最近 800 条+字段级合并覆盖（等价刷新最近约 3.2 年，复用第15条字段级合并机制）

### 33. 前复权早期价格为负 → 改用不复权
- 招商银行 2002 年 qfq 价格 -13.159 元（累计分红把早期价压成负值），无法用于历史数据研究
- **方案**：腾讯参数尾字段留空=不复权（`day,,,800,`），2002 年真实价 10.51 元
- 代价：送转股跳变未复权（如五粮液历史价有送股缺口）——真实价格口径，研究可接受

### 34. K 线行第 7 位是分红 dict 不是成交额
- 不复权模式下翻页到分红日附近，行尾出现 `{'nd':'2022','FHcontent':'10派17.38元',...}` 字典
- 直接 `float(r[6])` 崩溃（TypeError: float() argument must be string, not dict）→ 加 isinstance 判断

### 35. 增量 fetcher 未限制翻页 → 2004 前数据混入（自测发现）
- `full=False` 分支只写了一句“不翻页”的意图注释但**漏了 break** → 增量实际拉成全量，把 2002 年数据合并进缓存（新增 419 条“幽灵数据”）
- 教训：**写“只取一页”逻辑时必须显式 break**，注释代替不了控制流；自测断言 `n_new == 0` 抓出

### 36. 财报接口名试错：RPT_LICO_FN_CPD 返回 0 条
- 业绩快报接口对老股票返回空；**RPT_F10_FINANCE_MAINFINADATA**（主要财务指标）正确：101 条/只（1999-2026），含 EPSJB/BPS/ROEJQ/ZZCJLL + NOTICE_DATE（公告日）
- push2his K 线带 PE 字段的方案**仍被封**（RemoteDisconnected，与第1条同款）→ PE 历史只能由财报数据本地计算

### 37. 年报与一季报同日公告 → bisect 取错报告期
- 五粮液 2025 年报与 2026Q1 **同一天（2026-04-30）公告**，按公告日排序后 bisect 取到最后一条=年报 → PE 算成 33.81（用年报 EPS 代替 TTM）
- 修复：排序键改为 `(notice_date, report_date)` 双键，同日公告时取报告期最新；修复后 24.03 与腾讯 24.05 完全一致

### 38. 字典键切片不一致：构建 [5:7] 查询 [5:10]
- by_yq 键用 `report_date[5:7]`（"03"）而查询用 `[5:10]`（"03-31"）→ same_ly/annual_ly 永远 None → PE-TTM 全 None
- **教训：键的构造与查询必须用同一表达式**；此类 bug 靠“手工逐行复现通过 vs 函数返回 None”的对比定位

### 39. ROEJQ 是累计口径 + 接口缺失值标记为 0
- 招行 2025 年 ROE 序列 3.53→6.93→10.47→13.44——**累计（年初至今）** 不是单季；展示即东财 F10 口径，无需年化
- 2007Q1 的“修正记录”（新会计准则追溯调整，公告日 2008-04-23）ROEJQ=0.0 → 0 是接口缺失标记，`if roe` 判空转 None 留空

### 40. 用自洽性验证揪出“看似异常实则真实”的数据
- 五粮液 2025 年报 ROE 仅 6.89%（旧常识 20-30%）→ 一度怀疑计算 bug
- **自洽验证**：ROEJQ ≈ EPSJB÷BPS = 2.31/33.5 = 6.9% ✓ → 是真实业绩暴跌（白酒 2025 深度调整，EPS 8.21→2.31），非 bug
- 经验：**固定行业区间做校验会被基本面变化击穿，自洽性（多口径互算）才是稳的**

### 41. ExcelWriter 块内改单元格格式被 pandas 保存覆盖
- `with pd.ExcelWriter(...) as w:` 内直接改 `cell.number_format` 单独测试生效，实际导出不生效（pandas close 时按自己的格式规则重写）
- 修复：**导出完成后用 openpyxl 重新打开改格式再保存**（独立后处理）

### 42. Excel 日期显示时分秒 / 缓存 fetched_at 带时分
- pandas 写 datetime 默认格式 `YYYY-MM-DD HH:MM:SS`，Excel 打开显示带 00:00:00 → 后处理设 `number_format="yyyy-mm-dd"`（保留日期类型可运算）
- 缓存 fetched_at（拉取时间戳）按要求统一改为纯日期 `YYYY-MM-DD`，存量 42 个文件脚本截断

### 43. 多数据源单位不一致 → 表头统一带单位 + 换算
- 各源原始单位：腾讯 volume=手/amount=元（ETF与股票为估算）、中证官网 tradingVol=**股**/tradingValue=**亿元**、国证 volume=万手/amount=亿元
- Excel 表头统一：价格(点/元)、成交量(万手)、成交额(亿元)，按数据源分别除系数（腾讯/1e4、中证/1e6 等）；缓存保持原始单位不动

### 44. 缓存保护三件套（本会话加固）
- **原子写**：json.dump 直接写目标文件，中断即损坏 → 全部改 tmp + os.replace
- **删除同步安全阀**：md 解析结果与旧缓存差异 >50% 时（疑似 md 损坏）**禁止清空缓存**，只新增并预警——防止解析异常把 289 只缓存删光
- **损坏自愈**：JSON 损坏时检测并基于 md 重建；md 缺失友好报错不碰缓存
- 演练验证：删缓存自动全量重建、截断 JSON 自动恢复、md 异常不清空、正常小幅移除正常同步

### 45. PEG 的增长率口径抉择
- 未来预期增速无历史每日数据 → 用**历史 TTM EPS 同比**（本地可算，零新数据）；G≤0 留空（负增长 PEG 无意义）
- 实现细节：一年前日期用双指针 O(n)（日期单调，窗口索引单调不减），避免 O(n²) 每行回扫

### 46. pi 扩展机制踩坑（plan-mode / git-commit）
- `shift+tab` 被内置 `app.thinking.cycle` 占用且 `restrictOverride: true`（扩展无法覆盖，需用户改绑键位）
- skill（一次性指令）≠ extension（模式开关+事件拦截+工具），Claude Code 式 plan mode 必须是 extension；先写 skill 方向错误
- 手写 extension 后最终改用现成 npm 包 `@narumitw/pi-plan-mode`（更完整：/plan tools 工具选择器、plan_mode_question/complete、状态持久化）；安装第三方包需先审查源码（无网络/无子进程/无文件操作，纯 UI 指令型）
- git-commit 扩展（启发式生成 commit 信息）交付后被用户删除，勿重建

---

## 七、前端展示终端（web/）开发与优化阶段（08-04）

### 47. 单位换算双重除：前端把 manifest 因子又乘了一遍
- **现象**：指数板块图中/表格成交量小 1e4 倍、成交额接近 0；ETF/股票却正常
- **根因**：`prepareKline` 用 `volume / (vdiv * D.volume)`——manifest 的 vdiv/adiv 已是「原始→万手/亿元」的**完整除数**（与 Excel 同口径），前端又乘了 D（1e4/1e8）→ 双重换算
- **ETF/股票“恰好正常”掩盖了 bug**：它们不传 vdiv/adiv（默认 1），只乘 D 正好完成 手→万手
- **教训**：换算因子单一来源、只除一次；“某条路径恰好正常”可能是巧合而非正确，要逐源实测核对

### 48. const 块级作用域：maCount 定义在 else 块内，嵌套函数访问不到
- **现象**：股票板块下方表格整个不渲染，报 `maCount is not defined`
- **根因**：`maCount` 声明在 candlestick 分支的 `else` 块内（块级 const），外层嵌套函数 `setSubSeries` 引用它 → 运行时 ReferenceError → 后续表格渲染中断
- **修复**：提升到函数作用域顶层；`node --check` 只查语法查不出作用域问题，这类 bug 靠运行时报错定位
- **教训**：嵌套函数要共享的状态必须声明在函数级，不能藏在分支块里

### 49. 多行文本替换反复失败：先核对精确格式再大块替换
- 本会话多次 edit oldText 匹配失败：else 块内缩进是 4 空格（写成 2）、`{ name: 'K线'...` 实际是两行（`{` 单独一行）、注释行与预期不符
- **教训**：大块替换前先 `sed -n 'x,yp'` 或 `cat -A` 确认精确空白/换行；拆成小块逐一替换更稳；失败时用 repr 打印实际内容对比

### 50. pandas 全 None 列被推断为 object → 除法 TypeError
- 腾讯源指数成交额全 None → `pd.DataFrame` 该列 dtype=object → `/ adiv` 报 `Expected numeric dtype, got object`
- **修复**：除法前 `pd.to_numeric(col, errors="coerce")` 强制数值化
- 教训：pandas 对全 None 列不推断 float，数值运算前统一 to_numeric

### 51. tooltip 字段挤一行：float:right + 缺 <br> 混排
- 旧实现 `float:right` 加部分 `<br>` 分隔，"收"和"高"、"低"和"涨跌"之间无换行 → 同行拥挤错乱
- **修复**：统一 flex 行（字段名左、值右、一行一个字段），float 布局在 tooltip 小容器里不可靠

### 52. 指标曲线共享右轴的视觉压缩（PE 线“平”的真相）
- **现象**：陕西煤业 PE 线 2018 年后长达多年“平的”，用户认为数据错误
- **真相**：数据完全正确——2015-2017 煤炭亏损期 PE 波动于 -424~+1998（净利近零），共享右轴自动缩放到全量，2018 后正常区间（4~17）只占轴高 0.5%
- **尝试**：中位数±6×MAD 动态裁剪轴（正常区间占轴高升至 80%，极端值贴边）——修复有效但用户觉得“奇怪”选择回滚，保留原始缩放
- **教训**：多量纲指标共享轴的展示取舍要尊重用户直觉；数据正确≠显示合理，但“原始真实缩放”也是诚实口径

### 53. 环形图溢出与 canvas 裁剪：z-index 救不了 canvas 内文字
- 圆 radius 74% + center 34% → 左缘只剩 5px 贴边；hover 时 canvas 内 emphasis label 向左展开直接出界
- **关键认知**：canvas 内绘制的内容超出画布就被裁剪，**z-index 对 canvas 文字无效**（它不是 DOM）
- **修复**：hover 信息改由 tooltip 展示并 `appendToBody: true`（挂 body 顶层，永不裁剪），扇区 hover 用 `scaleSize` 放大作反馈；圆缩小为 62%、圆心移到 36%

### 54. ECharts canvas 性能三杀手（悬停延迟 + 拖动卡顿）
- **① K线 shadowBlur**：5000+ 根 K 线每帧高斯模糊（hover 十字线、dataZoom 拖动都触发整体重绘）——最大元凶
- **② 无降采样**：K 线 series 没配 `sampling: 'lttb'`，全量绘制
- **③ dataZoom 动画**：`animationDuration: 420` 未设 `animationDurationUpdate: 0`，拖动时每次更新都播过渡动画
- **修复**：去阴影 + K线 lttb + `animationDurationUpdate: 0`（保留首次入场动画）
- 经验：ECharts 大数据（数千点）交互卡顿先查这三项

### 55. 键盘光标起点三版迭代：最新日期 → 鼠标 hover → 点击锚定
- v1：首次按键从最新日期开始（用户：应该从鼠标位置开始）
- v2：mousemove + convertFromPixel 记录 hover 索引作起点（用户：键盘移动中鼠标动过还是旧位置；hover 持续更新会与键盘移动冲突）
- v3（定稿）：**左键点击锚定**——点击图表任意位置光标立即定位，方向键从锚定点移动；hover 仅在键盘未启动时记录起点；键盘移动中 hover 不打断（防鼠标抖动）
- 经验：交互起点需求一次问清；点击是最明确的“关注位置”信号，且不会与键盘连续移动冲突

### 56. 派生字段只加在副本上 → 统计卡恒为 0
- `_rec` 只 map 进 `all` 副本，统计卡却用原始 `rows`（无 `_rec`）过滤 → “入选20只推荐”恒显示 0；表格列看似正常是因为 applyFilter 立刻用 base 刷新掩盖了 bug
- 教训：派生字段加在副本上时，**所有读取方必须用同一数据源**；被“立即刷新”掩盖的问题要用不同路径（统计卡/表格）交叉核对

### 57. 过滤名单与 series 名不一致：'PE动' vs 'PE(动)'
- tooltip 去重名单写 `'PE动'`，指标曲线实际名称是 `'PE(动)'`（INDICATORS 的 label 带括号）→ 过滤失效，浮动面板出现带小图标的重复行
- 教训：名单匹配必须精确复制源定义（label），肉眼“看着一样”不算数

### 58. 其他小坑
- GBK 控制台打印 emoji 报 `UnicodeEncodeError`：脚本被 import 调用时 `sys.stdout.reconfigure` 不生效（只在 `__main__` 分支），打印前先包装 stdout
- 浏览器控制台 `chrome-extension://.../manifest.json` 报错是扩展噪音，与页面 bug 无关，排查时先排除
- `start "" file.html` 打开页面后，serve.py（no-cache）保证改 JS 后 Ctrl+Shift+R 即生效

---

## 八、前端增强与数据补全阶段（08-04 续）

### 59. 980092 无跟踪基金 → 季报重仓兜底方案直接否决
- 用户要求国证自由现金流（980092）成分“最次也要前十大”，原计划用跟踪产品的季报重仓兜底
- 天天基金搜索实测：`CATEGORYDESC="指数"`、`FundBaseInfo=null`、`StockHolder=[]`——**该指数没有任何跟踪基金**，兜底方案不可行
- 经验：先验证“产品存在性”再设计依赖它的方案

### 60. 遗留侦察脚本是金矿：接口 URL 直接复用
- 之前会话遗留的 `cnindex_member.mjs`（CloakBrowser 监听国证官网 980092 详情页、点击“样本详情”）直接给出接口：`www.cnindex.com.cn/sample-detail/detail?indexcode=980092&pageNum=1&rows=100`
- 且该接口**纯 HTTP 可访问**（UA+Referer 即可，无需 CloakBrowser），rows=100 一次拿全，返回代码/名称/国证行业/权重
- 经验：**遗留侦察脚本/缓存是资产**——CloakBrowser 监听脚本直接复用拿 URL，能纯 HTTP 就不必上浏览器

### 61. components.json 权重过滤 bug：权重 None 被当作“未入选”
- **现象**：159229 只有 10 只（932368 仅十大权重公开）、000151 变 0 只
- **根因**：`if w is None: continue`——权重为 None（未公开）的股票被当成“没入选”跳过，而实际是“入选但权重未公开”
- **修复**：键存在即入选，权重允许 None（前端显示 —）；修复后 159229→50 只、000151→30 只
- 经验：**“未公开”≠“不存在”**，两态必须区分

### 62. 名称括号归一化：半角 vs 全角括号后缀
- `fh.INDICES` 的名称带半角括号后缀（000151 为“上证国有企业红利(辅助,510720跟踪)”），而 md 解析的指数名已去全角括号（“上证国有企业红利”）→ 匹配 0 命中
- **修复**：匹配前 `split("(")[0]` 去后缀，并对 w 键同时兼容两种括号

### 63. 视图常驻后多图表并存：键盘监听互相干扰
- app.js 改为视图容器常驻（display 切换不销毁）后，4 个视图的图表实例同时存在 → 按方向键时隐藏视图的图表也响应
- **修复**：`kbdMove` 加 `if (!el.offsetWidth || !el.offsetHeight) return`（隐藏图表不响应）
- 同批实现 per-code 状态记忆（时间范围/日期/图表成分股tab），无记忆时继承当前 tab

### 64. 成分股汇总环形图动画反复播放
- 汇总板块每次搜索/筛选都 dispose+重建环形图 → 入场动画反复播；指数/ETF 板块只在切标的重建（体验不一致）
- **修复**：环形图数据固定（全量 base）后**仅首次创建**，筛选只刷新柱状图；重载（重试）时先 dispose 旧实例防挂空 DOM

### 65. 筛选器随结果重建 → “无法取消筛选”
- 若环形图随筛选结果重建，只剩被筛行业 → 扇区消失 → 用户无法再点击取消
- **设计原则**：筛选源（环形图）必须稳定显示全量，筛选结果（表格/柱状图）才可变化

### 66. 简介漏配 980092（配成 932368）
- 指数历史板块清单里是 980092（国证自由现金流），简介字典却配了 932368（中证800，159229 跟踪指数，不在该板块）
- **修复**：换键 + 写完整性校验脚本（板块清单 vs INTROS 键集合，无缺失无多余）
- 经验：**人工维护的映射表必须用脚本校验与源清单一致**

### 67. 验证脚本再踩 stdout 二次包装坑（第 19 条同款）
- 验证 update.py 新函数时 import update → 触发模块级 stdout 包装，再自己包装一次 → `I/O operation on closed file`
- **修复**：验证改用 `ast.parse` 静态检查函数定义，避免 import 带副作用的模块

### 68. Excel 代码列前导零：openpyxl 存文本，pandas 读取转 int 是读取行为
- openpyxl 写字符串 `'000651'` 存文本（Excel 显示正常）；pandas 读回显示 651 只是读取转换，虚惊一场
- 验证单元格原始类型用 openpyxl 直接读，别用 pandas 推断下结论

### 69. update.py 流程盲区 → 4 项改进
- **盲区**：web 前端数据包（_gen_web_data）与国证成分（_fetch_cnindex_components）是独立脚本，update.py 跑完不触发 → 前端展示旧数据；分红/财报“缓存存在就跳过”导致新公告永不刷新；md 数据日期硬编码 2026-08-02 永不更新
- **4 项改进（已实现）**：① 选项 7 前端数据包一键生成；② 选项 6 末尾联动（检测+数据包）；③ 选项 8 分红/财报检测（对比最新除权日/公告日，有变化才写）+ `--refresh-fin`；④ md 数据日期动态化
- 经验：**更新流程必须闭环**——采集→导出→前端展示一条链，任何中间产物单独跑都会被忘记

---

## 九、买卖区间分析功能开发阶段（S1-S8，08-05）

### 70. dy0 口径之争：TTM vs 官方年度快照（差 1.29 个百分点）
- 成分加权 TTM 股息率 4.22% vs 红利介绍.md 官方快照 5.51%——谁对？
- **无网络交叉验证**：ETF 累计净值反推真实分红 `近12月分红 ≈ (acc_nav增量 − nav增量) = 0.061元 → 4.28%`，与 TTM 加权 4.22% 吻合（差 0.06%）→ **TTM 口径正确**，官方是年度静态口径（上年度分红/现价）
- 教训：**口径冲突用独立数据源交叉验证**，别猜；ETF 累计净值是分红收益的天然探针

### 71. 股票分支把 dy 当价格序列传入反推函数（D=dy0² 扭曲）
- `build_stock` 误把 (date, dy) 序列传给为价格设计的 `build_from_close` → `D = dy_now×dy_now`（平方！）→ 序列变成 `dy0²/dy`（非线性扭曲+方向反转）
- **连带灾难**：S2 股票回测信号方向反了 → 得出“高股息陷阱负超额”错误结论；S3 打分把高股息股票（神华）判成“最贵”
- 修复后：股票 dy 分位全变（五粮液 6→94 分位、神华 98→1.5），回测股票超额由 -14% 变为 +0.6%~+5%（弱正有效）
- 教训：**同形函数对不同数据语义（价格 vs 比率）必须独立校验**；方向性错误最隐蔽——输出“看起来合理”但结论反了，要靠极端样本反查（五粮液股息率历史新高却 6 分位）

### 72. ETF rolling 窗口失效：信号全丢在 NaN 区
- ETF 上市仅 7 年（1614 行），1250 日滚动窗口有效区只有最后 354 天（2025 年后），2024-09 低点信号落在 NaN 区 → “无买入信号”
- **修复**：ETF 直接用跟踪指数的完整序列回测/分析（ETF 区间=跟踪指数区间）——dy0 加权结果自洽验证了映射（515180=4.22=000922、512890=4.91=H30269…全部吻合）
- 教训：**滚动窗口对短序列是隐性陷阱**，先检查有效区长度；同源映射可用数值自洽验证（加权dy相等）

### 73. 因子量纲不一致：trend/sent 返回 (pct, v) 与 dy/price 的 (v, pct) 相反
- S3 打分里趋势/情绪实际用乖离%、chg60% 原始值算分（量纲错），打印还显示成“趋势 0-3”误导——实际是乖离 -0.3%~+0.3%（趋势中性）
- **修复**：统一 f[k] = (v, pct) 结构；教训：**异构返回值协议必须统一并加测试**（T7 数值快照就是为此）

### 74. dy 分位方向：股息率高=便宜，与“0=便宜 100=贵”因子口径相反
- dy_pct 高=股息率历史高位=便宜，直接当“贵”因子用会把神华（dy 98 分位）判成最贵
- **修复**：factors.dy.pct = 100 − dy_pct（统一“0=便宜 100=贵”）；T3 断言验证反向已应用

### 75. D 漂移边界：反推 dy 绝对水平早期偏高，滚动分位不受影响
- 2014 年反推 dy=10.5%（真实约 5.5%，分红总额 12 年翻倍）——但滚动窗口内相对比较仍有效
- 验收：分位/信号语义正确（2014 信号日 dy=窗口 90+ 分位，信号后 1 年 +49%~+120%）；前端 dy 副图只画近 5 年规避漂移误导

### 76. ETF dy 副图无法前端重算（量纲矛盾）
- ETF 的 K 线是场内价（1.4 元），锚/股息率是跟踪指数点位（5459）——`dy_t = dyNow×closeNow/close_t` 前端算不出
- **修复**：后端给 ETF 存 `dy_series`（近 5 年），指数/股票前端重算；教训：**“前端重算省传输”的决策要按标的类型验证量纲**

### 77. addAnchorLines 被 setSubSeries 的 replaceMerge 覆盖
- 先 merge 锚线（markLine）到 series[0]，后 replaceMerge 整个 series 数组 → 锚线消失
- **修复**：setSubSeries 从 `chart.getOption()` 取当前主系列（含已合并的 markLine）；教训：**ECharts merge 与 replaceMerge 混用的状态管理要小心**，本地 option 变量 ≠ 实例内部状态

### 78. mount(el) 参数遮蔽 import 的 el 函数（TypeError 被内部 catch 吞掉）
- backtestView 的 `mount(el)` 参数遮蔽了 common.js 的 el() → 函数内 `el('div')` 报“el is not a function”，且被 mount 内部 catch 捕获显示为错误 UI，外层 try 拿不到异常
- **调试路径**：手动 mount 返回 OK 但页面失败 → 逐层拆解（模块导入→逐步执行片段→最终发现参数名遮蔽）
- 教训：**函数参数名避免与模块级函数同名**；被 catch 吞掉的错误要用“分步执行+单元验证”定位

### 79. 认真测试揪出两个隐蔽回归（S8 收尾）
- ① `setSubSeries` 的 replaceMerge 丢弃股票主图右轴 6 条指标 overlay（图例消失，默认隐藏肉眼难察）→ 从 getOption 提取 `yAxisIndex===3` 保留
- ② setSubSeries 后图例缺成交量/overlay（原代码 legend 就没含）→ 补全
- **测试方法论沉淀**：数据层 58 项断言（T1-T9 含数值快照/结构/边界/回测产物一致性）+ 前端 19 项浏览器矩阵（CloakBrowser 全板块遍历、视图常驻 DOM 按 offsetWidth 过滤可见性）；node --check 对 .js 按 CJS 解析抓不到模块语法错 → 复制为 .mjs 用 ESM 严格模式检查
- 教训：**每个新功能必须带回归测试**；overlay/图例这类“默认隐藏”的功能最容易被新改动静默破坏

---

## 十、ETF持仓链路与全量股票池扩展对齐（08-05 续）

### 80. “无跟踪基金”结论错误：自由现金流ETF持仓完全可获取
- 早期结论“980092 无跟踪基金 → 季报重仓兜底不可用”是**搜索方式错误**：东财搜索接口（searchapi.eastmoney.com/api/suggest/get）一搜 9 只同类 ETF（华夏159201/广发159229/嘉实159221/易方达159222…）全在
- 天天基金 F10 `FundArchivesDatas.aspx?type=jjcc`（需 Referer）直接返回季报前十大持仓+占净值比（纯HTTP）
- **验证**：159201 华夏 Q1 前十大（合计 54.24%）与 980092 官网权重（54.26%）完全同组同序 → ETF 完全复制指数，持仓占比≈指数权重

### 81. ETF 跟踪指数映射：之前猜错了（159229→980092 实为 932368）
- 持仓比对发现 159229 广发与 980092 官网 top10 只重合 6/10、159232 南方 5/10 → 跟踪的可能是其他指数
- **实锤**：东财基金详情接口 `FundMNewApi/FundMNBasicInformation?FCODE=xxx` 返回 INDEXNAME/INDEXCODE——159229→**中证800自由现金流(932368)**、159232→中证全指自由现金流(932365)、只有 159201 华夏等 7 只→**国证自由现金流(980092)**
- 教训：**映射关系用官方接口确认，别用成分重合度猜**；用户侧已修正：159229 替换为 159201 进入观察池

### 82. 东财分红接口单位坑：PRETAX_BONUS_RMB 是每10股派息
- 三一重工 2025 年度分红 3.6 实为每 10 股 3.6 元（每股 0.36）——直接当每股用会把股息率放大 10 倍
- **修复**：`bonus/10.0`；与项目 div_yield_calc 口径（_fetch_stock_data 第 373 行 `bonus10/10.0`）核对一致
- 教训：**新接口的单位必须与旧代码口径比对**（找同字段的既有除法）

### 83. 用户侧并行扩展：旧假设（40标的）需重新对齐
- 期间用户已并行完成两大扩展：股票池 289→329 全量（指标/分析覆盖 362 标的）、159229→159201 替换（932368 移除）
- 我的新脚本/断言基于旧状态设计（40 标的、159229）→ 对齐：ETF持仓链路兼容（159229 不在池内自动不生效）、测试断言由用户更新为 362、回测新增 `--full` 参数
- 教训：**多会话并行开发时，改动前先 git log/status 对齐现状**；脚本尽量对“标的集合变化”鲁棒（按数据驱动而非硬编码清单）

### 84. jjcc 接口解析坑：多季度/多表格混排
- `FundArchivesDatas.aspx?type=jjcc` 返回的 HTML 可能混入多个表格（其他季度/其他表），第一次解析正则抓到 20 行（两组）
- **修复**：`split("<table")` 只取第一个表格的数据行；ETF 打新碎股（0.01%）按占净值比 ≥0.5% 阈值过滤

### 85. 回测耗时与覆盖的权衡：--full 参数化
- 全量回测（362 标的 × 3 档滚动分位）约 15 分钟 vs 精选池（42 标的）约 1 分钟
- **设计**：默认精选池（快速日常），`--full` 全量（数据更新后建议定期跑）；回测脚本新增 full 参数由用户侧完成
- 全量结果：股票池 12M 超额 +1.39%（精选 20 只 +0.64%）——全量股票 dy 分位因子同样弱正有效

### 86. 测试断言硬编码标的数：40→42→362 三次变更
- T8 断言“三档各40标的”随标的池扩展连续挂了两次（40→42→362）
- **改进**：断言与产物自洽（by_p 行数 == analysis_dy 标的数）比硬编码数字更稳；测试文件里所有标的数断言应数据驱动

### 87. 上汽集团 dy=0.00 异常（精选池口径）
- 精选池 div_yield_calc=0.0，而东财 TTM 口径 0.62%（2025-07-31 除权 0.088 元/股）——疑似精选池除权日窗口判断差异
- 影响：159201 持仓加权 dy0 偏差约 0.03%（上汽权重 5.44%），可接受；已记录待查
- 教训：**权重大的成分股 dy 异常会传导到指数估算**，接入估算链路前应抽查权重前列个股

## 十一、回测报告全量扩展与其他成份股纳入（08-06）

> 背景：回测报告原先只覆盖“精选池+自选股”48 只，用户要求把股票历史板块中“其他成份股”分组（320 只）也纳入回测并展示在回测报告板块。实施后默认全量 368 标的、实测三档仅 24 秒；报告重构为五分组结构（指数/ETF/推荐20/其他成份股/自选股），测试 62 项全绿。
> 新洞察：其他成份股 261 只有效标的 12M 平均超额 +1.40%（正超额占比 54%）——大样本确认“dy 择时对个股整体弱正、远弱于指数/ETF”，S3 体系 B 设计得到支撑。

### 88. 需求指代没对齐：第一轮设计跑偏（“成分股”= 股票历史板块的“其他成份股”分组）
- 用户说“成分股也要放入回测报告中”，我按“全部指数成分股 + 行业统计”设计了 7 节方案（分组卡/行业维度/条形图），用户澄清：“我说的是**股票历史板块中**其他成分股”——即前端股票板块三个 tab（推荐20/其他成份股/自选股）里的“其他成份股”分组
- **修复**：重新聚焦——范围 = analysis_dy 全量（含其他成份股），行业统计等多余内容全部砍掉，只做“范围扩展 + 分组区分 + 展示”
- 教训：**需求中的名词先对齐准确指代**（哪个板块/哪个分组），设计文档开头先列“需求确认”小节请用户确认，避免方案跑偏返工

### 89. 耗时预估靠直觉翻车：文案“约1分钟”→“5-10分钟”→“约1分钟”来回改
- 设计时凭旧认知（48 只约 1 分钟）线性外推 368 只“5-10 分钟”，用户确认接受后把 update.py 文案全改成“约5-10分钟”
- 实施第一步做了 5 只标的基准测试：**单只仅 41ms**，全量三档实测 **24 秒**——纯计算 368 只 × 3 档只需秒级，原“约1分钟”含报告生成等固定开销
- 文案又全部改回“约1分钟”
- 教训：**耗时预估用基准测试不用直觉**，改范围前先跑 N 只测单只耗时 × 标的数 × 档位数；文案等实测后再写

### 90. 全量明细表可读性危机：367 行逐标的敏感性表无阅读价值
- 原报告结构：p85 明细（48 行）+ 逐标的敏感性表（48 行）+ 结论。全量 368 后：明细 367 行尚可接受（放附录），但“逐标的 × 三档 × 6 列”的敏感性表变成 367 行垃圾信息
- **重构**：敏感性表降为**分组粒度**（五组 × 信号数合计 + 12M 平均超额）；结论从“指数/股票”两分法改为**五分组统计 + 分档判定**（每组独立判断“有效/无效/高股息陷阱”）
- 前端同步：明细表加“分组”列（复用列筛选）+ 汇总卡下新增五分组卡（有效数/12M超额/正超额占比）
- 教训：**报告章节按读者视角分层**——正文给分组统计，明细放附录；“全量覆盖”不等于“全量表格都有价值”

### 91. 分组口径的重叠防御：推荐20 优先于自选股
- 分组判定时自选股清单（xlsx 现读 show==1）可能与推荐20 重叠（同一股票两边都出现）——若自选优先会把推荐股归进自选组，统计失真
- **修复**：`group_of()` 判定顺序 推荐20 > 自选股 > 其他成份股；指数/ETF 按 type 直接归类；scope 五组数量（11/11/20/320/6）与 manifest 分组核对一致

### 92. 池口径三处不一致：manifest 335 vs analysis_dy 346 vs 汇总表 309
- 回测范围设计时发现三个数字对不上：manifest 股票池 335（20 推荐 + 309 其他 + 6 自选）、analysis_dy 有 346 只股票（含 11 只已退池但指标文件残留）、“其他成份股”309 vs 320
- **统一口径**：回测以 analysis_dy（= web/data/stocks/*.json 指标文件）为准——它天然覆盖“有数据的全部股票”，退池残留自动剔除（无指标文件即无 dy 序列）
- 测试断言从“48 硬编码”改为动态 `len(by_p[p]) == 有 dy 标的数`，杜绝第四次改数字（第 86 条教训的再次实践）
- 教训：**多份清单并存时先选“唯一事实源”**，其余数字差异是派生口径，不要试图在代码里调和

### 93. 短期与长期信号方向相反：推荐20 的 6M -0.68% 但 12M +0.66%
- 全量结论五组判定时差点把推荐20 判为“无效”（6M 负超额），但 12M 为正——单看短期会误判
- **修复**：判定逻辑“6M 与 12M 双负才算高股息陷阱、12M 为正即有效、一正一负中性”，两个期限的数字都写进报告供读者自行判断
- 教训：**多期限交叉看再下结论**；策略判定给三态，不要单期限一刀切

### 94. 大样本验证结论的附加价值（成果记录）
- 其他成份股 261 只有效标的：6M +1.21%、12M +1.40%、12M 正超额占比 54%——与推荐20（+0.66%）同量级，都远弱于指数（+24%）/ETF（+35%）
- 至此“dy 分位择时对指数显著有效、对个股整体弱正”的结论从 26 只样本扩展到 300+ 只，S3 体系 B（个股 dy 低权重 + 估值/趋势配合）的设计得到大样本支撑
- p95 档总平均超额 +7.37% > p90 +2.57% > p85 +3.01%：阈值越严买点越极端、超额越高，三档方向一致（策略稳健）

---

## 关键经验清单

1. **东财按子域隔离风控**：push2 封了不代表 datacenter-web 也封，可降级切换
2. **CloakBrowser 监听网络请求**是逆向 SPA 数据接口的最快路径，远快于逆向 JS
3. **数据正确性靠交叉验证**：ETF 持仓 vs 指数权重比对，揪出 159229 跟踪指数错误
4. **写缓存前先想覆盖语义**：拉取失败返回 0 条时绝不能覆盖已有好缓存；合并新旧数据必须先统一字段规范（此坑踩过两次）
5. **增量更新必须字段级合并**：新行整体覆盖旧行 = 一次不完整拉取就能毁掉全部缓存；新值为空时保留旧值
6. **官网文件权限策略差异**：中证官网部分指数（000151/932365/932368）样本文件不公开（403），需东财成分表+官网十大权重拼装
7. **接口分页/频率参数陷阱**：lsjz 的 pageSize 被忽略、新浪 datefrom 不生效、index-perf 有隐藏限流——先小样本实测再全量
8. **可被 import 的脚本不要做模块级副作用**：stdout 包装等只放 `__main__`，否则被其他模块 import 时二次包装崩溃
9. **工具脚本谨慎用 sed 全局替换**：多文件多位置替换前先确认目标位置，替换后立即检查
10. **push2delay 是 push2 被封时的同协议降级**：但字段不全（无 f9/f23/f133），估值走腾讯、股息率走 datacenter
11. **北交所 92 号段 prefix 路由必须优先于 9x**：此坑踩过两次，写路由函数时按文档顺序逐条核对
12. **分红接口金额单位是“每10股”**：`PRETAX_BONUS_RMB` 必须 ÷10 才是每股；股息率算出来 40-50% 就是单位错了
13. **“窗口内无派息”记 0 而非空**：数据缺失与数值为 0 要显式区分
14. **长批量任务三步法**：先单请求探测连通性 → 连续失败快速中止（8次）→ 每 N 条自动存盘；不要干等，等再久也大概率是 IP 被封
15. **缓存同步以“唯一事实源”为准**：多份缓存间派生字段（pe/pb/mcap）重建时从全量缓存重算，不复制旧表
16. **可被 import 的脚本必须函数化 + `__main__` 保护**：顶层副作用代码无法被 update.py 复用
17. **改完代码立即编译验证**：`python -c "import py_compile; py_compile.compile('x.py', doraise=True)"` 防缩进/语法事故
18. **腾讯 K 线 start 参数无效、end 翻页无缝**：全量用 end 往前翻；增量=最近800条+字段级合并
19. **前复权会把长分红股早期价格压成负值**：历史价格研究用不复权（尾参留空），送转缺口是真实口径的代价
20. **“只取一页”必须有显式 break**：注释代替不了控制流，增量漏 break 会拉成全量混入范围外数据
21. **财报同报告期可能有多条记录**（修正/追溯调整公告）：排序键用 `(公告日, 报告期)` 双键；字典键构造与查询用同一表达式
22. **东财主要财务指标字段语义**：EPSJB=累计口径、ROEJQ=加权累计ROE、ZZCJLL=总资产净利率、BPS=期末每股净资产；缺失值标记为 0 而非 null，判空用 `if v` 不能 `is not None`
23. **固定区间校验会被基本面击穿，自洽性（多口径互算）才是稳的**：五粮液 ROE 暴跌是真实数据，ROEJQ≈EPS÷BPS 交叉验证
24. **pandas ExcelWriter 块内改格式会被保存覆盖**：日期/数字格式统一导出后用 openpyxl 独立后处理
25. **多数据源单位必须先实测再统一**：同是“成交量”，腾讯=手、中证=股、国证=万手；表头带单位+按源换算，缓存保持原始单位
26. **缓存保护三件套**：原子写(tmp+replace) + 删除同步安全阀(差异>50%禁删) + 损坏自愈(基于md重建)；测试脚本演练全部场景
27. **PEG 历史口径只能自算**：无历史预期增速数据，用 TTM EPS 同比；负增长留空；双指针 O(n) 避免 O(n²)
28. **pi 扩展与 skill 是不同机制**：模式类功能用 extension（事件+工具+持久状态）；shift+tab 被内置保留无法覆盖（restrictOverride）；第三方包安装前审查源码
29. **单位换算因子单一来源、只除一次**：manifest 的 vdiv/adiv 已是完整除数，前端不得再乘；"某条路径恰好正常"可能是巧合，逐源实测核对
30. **ECharts canvas 性能三杀手**：shadowBlur 阴影模糊（数千图形每帧高斯模糊）、series 无 sampling 降采样、dataZoom 拖动未禁动画（animationDurationUpdate: 0）
31. **canvas 内文字溢出无法用 z-index 解决**：canvas 裁剪发生在绘制层，hover 信息用 DOM 层 tooltip（appendToBody: true）
32. **嵌套函数共享状态必须声明在函数级**：const 藏在分支块里，外层函数访问即 ReferenceError
33. **派生字段副本与原始数据要一致**：字段加在副本上，所有读取方（统计卡/表格）必须用同一数据源
34. **交互起点需求一次问清**：键盘光标起点"从鼠标位置"多次迭代后定稿为点击锚定（最明确且不与键盘移动冲突）
35. **大块文本替换前先确认精确格式**：缩进/换行/注释差异会导致 edit 反复失败，先 sed 预览再替换，小块多次更稳
36. **多量纲指标共享右轴会视觉压扁**：极端值（亏损期 PE ±2000）把正常区间压成平线——数据正确但显示误导；裁剪轴方案需用户认可，尊重用户对“原始真实缩放”的偏好
37. **遗留侦察脚本/缓存是资产**：CloakBrowser 监听脚本直接复用拿接口 URL；能纯 HTTP 访问就不必上浏览器
38. **“权重未公开”≠“未入选”**：键存在即入选，权重允许 None 显示 —；两态必须区分
39. **人工维护的映射表必须脚本校验**：简介字典 vs 板块清单（键集合比对，无缺失无多余）
40. **视图常驻（display 切换）时全局监听要加可见性判断**（offsetWidth/offsetHeight）；图表实例销毁防挂空 DOM
41. **筛选源必须稳定**：环形图若随筛选结果重建会消失导致无法取消，筛选源显示全量、结果才可变
42. **验证脚本优先 AST/静态检查**：避免 import 带副作用（stdout 包装）的模块踩二次包装坑
43. **更新流程必须闭环**：采集→导出→前端数据包一条链；财报/分红“缓存存在就跳过”会有新公告永不刷新的盲区，需要检测机制（对比最新公告日）
44. **口径冲突用独立数据源交叉验证**：ETF 累计净值反推分红收益（acc_nav增量−nav增量）验证股息率口径，别猜谁对
45. **方向性错误最隐蔽**：同形函数对不同语义（价格 vs 比率）复用会 D² 扭曲+方向反转，输出“看起来合理”结论却反了——用极端样本反查（股息率历史新高却低分位）
46. **滚动窗口对短序列是隐性陷阱**：先检查有效区长度；短历史标的用同源长序列（ETF→跟踪指数），映射可用加权值自洽验证
47. **异构返回协议必须统一**：f[k]=(v,pct) 单一结构 + 数值快照测试（T7），防止量纲/方向悄悄错
48. **“前端重算省传输”按标的类型验证量纲**：ETF 场内价 vs 指数点位不同量纲，后端存序列更稳
49. **ECharts merge/replaceMerge 混用是状态陷阱**：本地 option 变量 ≠ 实例内部状态；replaceMerge 前从 getOption() 取当前系列
50. **函数参数名避免与模块级函数同名**（mount(el) 遮蔽）；被 catch 吞掉的错误用分步执行+单元验证定位
51. **每个新功能必须带回归测试**：overlay/图例等“默认隐藏”功能最容易被新改动静默破坏；node --check 对 .js 按 CJS 解析，ESM 语法错要用 .mjs 扩展名检查
52. **“无跟踪基金”结论先质疑搜索方式**：东财 suggest 接口一次搜到 9 只同类 ETF；ETF 季报持仓（jjcc 接口）完全可获取且占净值比≈指数权重
53. **映射关系用官方接口确认，别用重合度猜**：东财 FundMNBasicInformation 直接返回跟踪指数代码（159229→932368 猜错实锤）
54. **新接口单位必须与旧代码口径比对**：PRETAX_BONUS_RMB 每10股 vs 每股，找同字段既有除法确认
55. **多会话并行开发先 git log/status 对齐**：脚本按数据驱动（遍历实际文件）而非硬编码清单，标的集合变化时自动适配
56. **HTML 表格解析先确认表格边界**：多表格混排时取第一个 table；ETF 打新碎股按阈值过滤
57. **耗时操作先基准再分档**：回测曾按“默认精选池（1分钟）vs --full 全量（15分钟）”分档，实测单只 41ms 后改为默认全量（368 只 × 3 档仅 24 秒），--full 参数删除；分档决策应基于实测而非线性外推
58. **测试断言与产物自洽而非硬编码**：标的数断言改为动态比对（by_p 行数 == 数据源标的数），池扩展不再频繁改测试
59. **指数估算链路抽查高权重成分**：权重前列个股的 dy 异常（如 0.00）会直接传导到指数 dy0，接入前应人工核对
60. **需求名词先对齐指代**：设计文档开头列“需求确认”（哪个板块/哪个分组），确认后再展开方案；“成分股”这类泛指名词在不同语境指不同集合，第一轮方案跑偏的根因就是没对齐
61. **耗时预估先基准后文案**：改范围/批量前跑 5 只测单只耗时；文案按实测写别拍脑袋——本次 48→368 只反直觉地“约1分钟”变“24秒”，文案来回改
62. **大报告按读者视角分层**：正文=分组统计与判定，附录=全量明细；逐标的敏感性表在 300+ 行时降为分组粒度；前端分组卡与后端分组常量保持同一来源（GROUPS 元组）
63. **判定给三态别一刀切**：双负=陷阱 / 12M 正=有效 / 一正一负=中性，多期限数字全量呈现；抽查重跑（1 只标的重算 vs json 行）比只查结构更能发现跑批漏算

## 十二、推荐20量化评分与动态化改造（08-07）

### 95. 股息率“空窗假归零”：滚动 365 天窗口的固有缺陷
指标文件 dy 口径为“除权日在(当日−365天,当日]内的派息合计÷收盘”。**除权日顺延/披露延迟时窗口内可能无任何除权日** → 高股息股 dy 瞬间归零（粤高速 0.0、上汽 0.0、中集 0.0——全是假零）。硬过滤拿 dy 一刀切时会误杀整批好股票（145 只被过滤里有 13 只 dy<0.5% 属此类）。
修复：**窗口内无除权时回退最近一次已除权单次派息**（防假归零；一年两派的股票不受影响）。
教训：滚动窗口口径的“边界悬崖”要用空窗回退兜底；修复后必须**全链路重算**（指标→analysis→回测）——analysis 股票分支直接读指标文件，两者口径由测试断言锁定，改一处不重跑 ga.main() 会连环红。

### 96. 分红趋势 TTM 口径误判特别分红
初版 B5 用“近 365 天派息 vs 前 365 天”同比——**神华 2025-07 有 22.6 元/10股特别分红，2026 年没有 → TTM 同比 −68% → 4 分重罚** → 神华/五粮液/长电这类“历史分红慷慨”的蓝筹全被压到 100+ 名。
修复：**改年度口径**（按 ex_date 年份聚合，最新完整年度 vs 前一年度）——特别分红被单年吸收，不再跨年对比失真。

### 97. 分红率分母用错周期（单季 eps 虚高 4 倍）+ 财报缓存数据异常保护
初版 B4 = 年度派息 ÷ 最近一期 eps——财报 rows[0] 是 Q1 单季 → 分红率虚高 4 倍（招行 135%、神华 194%）。修复：**用最新年报 eps**。
又发现五粮液 2025 年报缓存 np=89.5 亿（真实应 ~300 亿，roe 从 23%→6.9% 不可能）——源数据失真 → B4 出现 360% 荒谬值。修复：**B4>150% 视为数据异常置 None 不参与评分**（宁缺勿错）。

### 98. 行业约束挤出高分股：备选 10 只全是银行
“行业≤3”把银行压到 3 只后，**备选 10 只全部是银行（64-66 分）**，而榜单 14-20 名只有 59-61 分——约束的强副作用（银行是当前红利池里估值最低、分红最稳的板块，评分天然最高）。
修复：**四象限保底**（金融/防御/周期/消费各 ≥3，象限内按总分、受行业≤4 限制；保底只决定选谁，名单仍按总分重排）——周期 1 只→3 只（中创智领/云天化/芭田）。
教训：约束类规则要检查“被挤出的群体画像”——备选全同质就是约束与评分冲突的信号。

### 99. fsd.STOCKS 动态化：产物单一来源 + 回退 + 防循环导入
推荐清单从硬编码改为读 `cache/_推荐20.json`（评分产物）。关键点：
- **产物缺失/损坏回退硬编码清单**（数据层脚本不能因评分产物缺失而崩）
- **避免循环导入**：不能 import `_fetch_pool_data`（它 import fsd），tcode 前缀自己写（6/9→sh 其余 sz）
- 一处改造自动跟随：manifest rec 标记 / 回测“推荐20”分组 / 股票 Excel 导出 / K线增量拉取
- 验证闭环：重跑推荐 → build_manifest → 重跑回测 → 浏览器确认推荐 tab 第一只=江苏银行、回测组显示 +10.76%

### 100. bash heredoc 与行级补丁的两个工具坑
- **未加引号 heredoc 中 `\n` 被 bash 转义成 `\n`** → python 源码字符串提前闭合 → SyntaxError “unterminated string literal”——heredoc 一律用 `<<'EOF'`（引号包裹），或避免在补丁字符串里写转义序列
- **行级插入缩进必须跟随目标行**：ga.main() 在 if 块内（8 空格）与外层（4 空格）缩进不同，统一 4 空格插入会 IndentationError；先 git checkout 恢复被污染的 update.py 再用“缩进跟随”重打，挂载数用断言校验（应 5 处）
- 多次失败补丁会残留污染，**重试前先 git checkout 目标文件**再打

### 101. 新功能回测的“对照组”价值（成果记录）
量化评分推荐 20 只 vs 旧人工池：**12M 信号超额 +0.66% → +10.76%**，正超额占比 68% → 72%（p90 档）——同样的回测框架、同样的信号规则，唯一变量是清单本身。选股逻辑的提升被直接量化呈现，这是“对照组设计”在策略迭代中的说服力所在。

## 十三、组合回测与前端页（08-07）

### 102. 组合回测的定位：验证“选股逻辑”而非“择时”
现有择时回测（backtest.json）验证的是“何时买卖”的信号超额；组合回测（portfolio_backtest.json）验证的是“选哪些股票”的持有收益。两者正交，方法论必须分开标注。
对照组设计（三基准）：000922 价格指数（大盘基准）/ 旧人工20只固定清单（**人工 vs 量化的直接对照**）/ 候选池等权（**分离“硬过滤贡献”与“精选贡献”**）——候选池等权 +54.5% vs TOP20 +56.9%，差值 2.4pp 即精选的增量；vs 000922 的 +40.7pp 大部分来自 dy≥3% 过滤与池内选择，这是解读结果的关键结构。

### 103. 结果解读的诚实性：交易成本必须入表
量化 TOP20（+56.9%）vs 旧人工 20（+53.0%）仅 +3.9pp，且量化季度换仓（平均换手 43%）有成本、旧人工零换仓——**差额需扣除交易成本后看待**（估算约 1pp，报告明示）。教训：对照组收益对比必须同时披露换手率与成本假设，否则“超额”会误导；换手率从相邻期持仓重叠度直接计算（1 − 交集/20），几行代码但让结论可信。

### 104. 新脚本骨架的三类低级错误
- **元组不可变**：`px = [(date, close, amount, bonus)]` 后 `r[3] = ...` → TypeError——新脚本批量构造行数据时用 list 而非 tuple
- **常量遗漏**：pick_at 里用 `AMT_DAYS`（在别的脚本里定义了）→ NameError——新脚本的常量要自查清单（窗口/阈值/路径）
- **收集列表遗漏**：统计了 `nav_pool` 但忘记收集 `navs_pool` → NameError 在最后一步——新脚本写完后先跑通“最小闭环”（哪怕只输出 1 期）再上全量

### 105. bash heredoc 补丁连环坑（本次最耗时）
给脚本打补丁时反复踩转义：
- 未加引号 heredoc：`\n` 被 bash 转成 `\n`（换行）→ python 源码字符串提前闭合 → SyntaxError
- 加引号 heredoc：`'print("\n═══")'` 里的 `\n` 在 python 里是字面 `\n`（反斜杠+n）→ 与文件里的 `\n` 匹配失败 → AssertionError
- 嵌套 `.replace('\\n','\n')` 再被 bash 处理 → 彻底混乱
**终极解法：补丁字符串含转义序列时直接用 edit 工具（精确文本匹配，无 bash 中间层）**——三次 heredoc 失败后换 edit 一次成功。教训：bash → python 双重转义是叠加态，能绕开就绕开。

### 106. 前端新视图的两处注册点
- **app.js `VIEWS` 白名单**：hash 路由 `currentView()` 对白名单外的值回落 'index'——新视图不注册，点击 tab 页面不切换（表现为“停在指数历史”），且无任何报错
- **renderTable 的 fmt 仅支持字符串**：返回 DOM 元素会被 `td.textContent = fmt(...)` 转成 `[object HTMLSpanElement]`——单元格富文本要用纯文本+符号（▲▼），或给 renderTable 扩展富文本支持
- 两处都是“静默失败”（页面看起来正常但不工作/显示错误内容），浏览器矩阵测试能抓到（断言列内容而非只看行数）
